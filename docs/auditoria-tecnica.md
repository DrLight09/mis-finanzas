# Auditoría técnica — mis-finanzas (index.html)

Reporte basado en verificación directa del archivo.

> Este documento es distinto a los demás: no describe cómo funciona un módulo ni un bug ya corregido. Son hallazgos de seguridad/arquitectura **todavía pendientes** de resolver — por eso vive aparte y no en `CHANGELOG.md` (que es solo para lo ya corregido). A medida que se resuelve cada punto, se mueve al `CHANGELOG.md` bajo la sección "Infraestructura / seguridad" y se borra de acá.
>
> **Nota de esta revisión (2026-07-16):** se resolvió una buena parte de los hallazgos críticos (CSP, `setDoc` sin manejo de errores, contraste, viewport, SEO, parte del rendimiento) — el detalle de qué se hizo y por qué vive en [`CHANGELOG.md`](./CHANGELOG.md#infraestructura--seguridad). Este documento quedó reescrito para reflejar solo lo que sigue pendiente, más lo nuevo que apareció en el camino.
>
> **Nota adicional (2026-07-16, misma fecha, sesión distinta):** se construyó el sistema reusable de eventos (`js/core/events.js`) y se migró Spotify como primer módulo completo (`onclick` → `data-action`, extracción a `js/modules/spotify.js`, 3 casos nuevos de `.innerHTML` sin escapar corregidos). Detalle en [`CHANGELOG.md`](./CHANGELOG.md#infraestructura--seguridad). Puntos 1, 2 y 3 de abajo actualizados con el avance real.
>
> **Nota adicional (2026-07-17):** se migró Mesada como segundo módulo completo, mismo patrón que Spotify — con dos diferencias: no hizo falta partir el archivo en dos (una sola dependencia real, `crearSplitWidget`, ya definida donde hacía falta), y aparecieron 2 controles estáticos con `onclick`/`onchange` inline que no vivían en las plantillas del módulo (quedaban fuera del barrido). También se repitió el mismo hallazgo de `.innerHTML` que en Spotify (texto libre envuelto en una función, esta vez `fuenteLabel()`), 3 casos corregidos. Detalle en [`CHANGELOG.md`](./CHANGELOG.md#infraestructura--seguridad). Puntos 1, 2 y 3 de abajo actualizados otra vez.
>
> **Nota adicional (2026-07-18):** se migró Encargos como tercer módulo completo. Como Spotify, necesitó dos archivos (`encargos.js` + `encargos-personas.js`) por la misma razón de orden de carga: la integración con Personas depende de funciones definidas más abajo en `index.html`. Se repitió otra vez el mismo hallazgo de `.innerHTML` sin escapar vía función auxiliar (`fuenteLabel()`, `iniciales()`) — esta vez ~18 casos, el conteo más alto de los tres módulos migrados hasta ahora, lo que refuerza que no es un caso aislado. Detalle en [`CHANGELOG.md`](./CHANGELOG.md#encargos). Puntos 1, 2 y 3 de abajo actualizados otra vez.
>
> **Nota adicional (2026-07-20):** al empezar esta migración se confirmó que **Préstamos ya estaba migrado también** (namespace `data-action="prestado:..."` ya presente en `index.html`), aunque este documento nunca lo había registrado — quedó como una nota suelta pendiente de corregir el día que se audite ese módulo de nuevo; no se investigó más a fondo porque no era el alcance de esta sesión. Se migró **Tarjetas de Crédito** como módulo nuevo — a `js/modules/tarjetas_credito.js` — con dos particularidades frente a los tres anteriores:
> - **23 `onclick`, no 14:** el barrido por nombre de función encontró 14 en el HTML generado dinámicamente, pero había 9 más en HTML estático (8 círculos de color del selector + el botón persistente "Agregar tarjeta" del listado) que ese barrido no agarra — mismo problema ya descrito en la nota de Mesada ("controles estáticos... quedaban fuera del barrido"), confirmado una segunda vez.
> - **El `<script>` original no se pudo extraer como bloque contiguo:** compartía el tag con `navTo()` (navegación global de toda la app, no de TC) y con el "Feed de actividad financiera" (módulo sin relación con TC que vivía ahí por casualidad de cómo se fue armando el archivo). Ambos se quedaron en `index.html`; solo se extrajo lo que era realmente de TC.
>
> Se repitió otra vez el hallazgo de `.innerHTML`/`toast()` sin escapar vía función auxiliar — esta vez 5 sitios (`tc.nombre` y `fuenteLabel()` interpolados directo en `toast()`, `fuenteLabel()` y una variable `descPago` sin escapar en el detalle de un pago, y el `<option>` del selector de cuenta de pago). Cuarta confirmación seguida del mismo patrón. Detalle en `CHANGELOG.md#tarjetas-de-credito`. Puntos 1, 2 y 3 de abajo actualizados otra vez.
>
> **Hallazgo nuevo, todavía sin resolver:** el mismo patrón (`f.label` sin escapar) existe también en `buildFuentesOptsHtml()` — función núcleo compartida por TODA la app (no solo por TC) para poblar selectores de cuenta. No se tocó esta sesión por el mismo criterio que ya se aplicó a `toast()`: es núcleo compartido, y cambiarlo de paso podía afectar pantallas fuera de este alcance. Se agrega como hallazgo puntual nuevo abajo, en el punto 2.
>
> **Nota adicional (2026-07-22):** se migró **Cuentas** — el módulo más grande extraído hasta ahora (agrupa Nequi, Efectivo, cuentas personalizadas y todo el subsistema de Nu: cajitas, tasa EA con historial por tramos, CDTs y metas de ahorro), a un solo `js/modules/cuentas.js`. No hizo falta partirlo en dos como Spotify/Encargos: no depende de nada definido más abajo en el documento.
> - **27 `onclick`:** 17 en HTML generado dinámicamente (cajitas, CDTs, metas, filtros de movimientos) + 10 en HTML estático de `screen-cuentas` que el barrido por nombre de función no agarra — quinta vez que aparece este mismo problema (ya visto en Mesada, TC). De los 27, dos (`abrirDetalleMov`/`eliminarMovimiento`) se registraron bajo el namespace `core:` en vez de `cuentas:`, porque esas dos funciones las usa toda la app (el feed general de actividad), no solo Cuentas — quedaron en `index.html`, no en el módulo (extraídas a `js/core/movimientos.js` el 2026-07-26, ver nota de esa fecha en el punto 3 más abajo).
> - **Mismo problema de "código no relacionado compartiendo `<script>`" que en TC:** el preview del sheet "crear CDT" vivía en el mismo bloque que Tarjetas de Crédito, sin relación real con TC — se movió a `cuentas.js`.
> - Se repitió una quinta vez el hallazgo de `.innerHTML`/`toast()` sin escapar vía función auxiliar — esta vez **12 sitios** (nombre de cuenta personalizada, nombre de cajita, nota/descripción libre de un movimiento, y `fuenteLabel()` sin escapar en el sheet de transferir). Detalle en `CHANGELOG.md#cuentas`.
> - **Código muerto encontrado (no se tocó):** `toggleCDT()`, `toggleCajita()` y `_expandCajitaCDTs()` ya no los llama nadie — trabajan sobre ids que el render actual de cajitas ya no genera. Se anotó en `cuentas.md`, no se borró de paso.
> - Se documentó el módulo por primera vez en `cuentas.md`, con ayuda de `guia-estilo-sheets.md` para el inventario de sheets — se notó que el sheet `chequeo-nu` (corrección manual de saldo de Nu) no está en el inventario de esa guía; queda pendiente agregarlo ahí.
>
> Puntos 1, 2 y 3 de abajo actualizados con este avance.
>
> **Nota adicional (2026-07-22, misma fecha, sesión distinta):** se migró **Inicio** (dashboard) a `js/modules/inicio.js` — sexto módulo migrado, con una diferencia real respecto a los cinco anteriores: **`screen-inicio` no tiene ningún `onclick` inline** (es una pantalla de solo lectura/resumen, sin formularios propios), así que el punto 1 no tuvo nada que migrar acá — la cuenta de `onclick` restantes no baja por este módulo, a diferencia de los cinco anteriores.
> - Se repitió por **sexta vez** el hallazgo de `.innerHTML` sin escapar vía función auxiliar — esta vez 1 solo sitio: `spNombreDe(p)` interpolado directo en `renderAttencion()` (aviso de cobro de Spotify vencido). Corregido envolviéndolo en `escHtml()`. El conteo bajo (1, contra los 12 de Cuentas o los 18 de Encargos) es esperable: Inicio es una pantalla derivada/agregada, no un lugar donde el usuario escribe texto libre nuevo.
> - Se extrajeron: `renderAttencion()` ("Necesita atención"), `calcHealthScore()` + `renderHealthScore()` (salud financiera), `renderProyeccion()` (proyección financiera), `_checkGastoAlto()` + su hook a `refresh()` (aviso "Gastos altos" en el hero), y el IIFE que reposiciona la sección "Necesita atención" justo debajo del hero.
> - **Se dejó `refresh()` en `index.html`**, a propósito: es el orquestador central compartido por las 13 pantallas (actualiza cuentas, gastos, deudores, mesada, spotify, etc., no solo Inicio) — mismo criterio ya aplicado con `navTo()` al migrar TC. También quedó en `index.html` el hook `_renderMejoras()`/`_hookRefreshMejoras()`: además de llamar a `renderHealthScore()`/`renderProyeccion()` (Inicio), ese mismo hook dispara `renderPresupuestos()` (módulo de Análisis) — es infraestructura compartida entre dos módulos, no se puede partir sin tocar Análisis, fuera del alcance de esta sesión.
> - `index.html` bajó de 11.381 a 10.934 líneas (-447). Se documentó el módulo por primera vez en `inicio.md`.
>
> Puntos 1, 2 y 3 de abajo actualizados otra vez.

> **Nota adicional (2026-07-23):** esta sesión no migró módulos nuevos — encontró **tres migraciones ya hechas que este documento nunca había registrado**: `js/modules/gastos.js`, `js/modules/plata_comprometida.js` y `js/modules/alcancia.js` ya existen y ya están cargados desde `index.html`, con namespaces `data-action="gastos:..."`, `"cp:..."` y `"alcancia:..."` propios. Mismo caso ya anotado el 2026-07-20 con Préstamos ("quedó como nota suelta pendiente de corregir"): el trabajo se hizo, el documento no. Se revisaron los tres archivos directamente para dejar la cuenta al día:
> - **Gastos** (variables + fijos): archivo único, no depende de nada definido más abajo — mismo caso que Mesada. **6 `onclick` migrados dentro del módulo** (filtro de mes, eliminar gasto variable, eliminar y pagar gasto fijo, los dos botones de estado vacío) + **2 más que vivían como controles estáticos en el sheet `pagar-gasto-fijo`** (un `onclick` y un `onchange`, no en plantillas del módulo) — estos dos se quedaron en el bloque de wiring centralizado de `index.html`, siguiendo la misma convención ya usada para controles estáticos en TC/Cuentas. Repite el hallazgo de `.innerHTML`/`toast()` sin escapar vía `fuenteLabel()`/`tc.nombre` — **4 sitios corregidos** (badge de fuente, 2 `toast()` de error en `addGastoVar`, `toast()` de compra cargada a TC) + un quinto en el `<option>` del selector de `abrirPagarGastoFijo()` (reimplementaba su propio selector a mano en vez de `poblarFuente()`, mismo hallazgo puntual que ya tenía TC).
> - **Plata Comprometida**: archivo único, mismo caso que TC (no depende de Personas, usa `S.deudores` directo). **27 onclick/onchange migrados** (26 onclick + 1 onchange sobre un `<select>` oculto que en la práctica nunca se disparaba) — el conteo más alto de los ocho módulos migrados hasta ahora. El propio archivo trae un detalle que vale la pena registrar: su comentario de cabecera todavía dice "pendiente agregar `<script src=...>` en `index.html`" como tarea sin hacer — pero **ya está agregado** (verificado en `index.html`, justo después de `js/core/events.js`), así que ese comentario quedó desactualizado por una sesión posterior que sí lo hizo. Repite el hallazgo de `.innerHTML` sin escapar vía `_cpFuenteLabel()` — **6 sitios corregidos** (`item.cuentaDestino` en la card principal, `tc.nombre` en dos ramas del plan de "Recibir", `_cpFuenteLabel(d.gastoCajita)` en las dos alertas de "Necesita atención", y `errores.join(', ')` en un `toast()`). Los usos de `_cpFuenteLabel()` dentro de `logCambio(...)` no se tocaron, mismo criterio que con `toast()` en TC (feed de actividad, escapa en su propio renderer, núcleo compartido fuera de alcance).
> - **Alcancía oculta**: archivo único, depende de `getSaldoFuente`/`sumarFuente`/`buildFuentesOptsHtml` ya definidos más arriba — mismo caso que Mesada. A diferencia de Plata Comprometida (que inyecta toda su pantalla por JS), acá la pantalla (`screen-alcancia`) es HTML estático que ya vivía en `index.html` con sus tres `data-action="alcancia:..."` ya wireados (`iniciarNueva`, `abrirDepositar`, `abrirDestapar`); solo los sheets (`alcancia-depositar`, `alcancia-destapar`, `alcancia-resultado`) se inyectan dinámicamente desde el módulo — un patrón híbrido no visto en los ocho módulos anteriores. **6 acciones registradas** bajo el namespace `alcancia`. A diferencia de los siete módulos anteriores, **no se repitió el hallazgo de `.innerHTML` sin escapar en código vivo**: el único `escHtml()` del archivo (`m.desc`) y la única interpolación sin escapar encontrada (`fmtFuente`, nombre de cajita/cuenta libre) viven dentro de un bloque de "movimientos actuales" completamente deshabilitado con `if(false)` (comentario: "movimientos solo visibles en las cuentas de origen") — **código muerto no tocado**, mismo criterio que `toggleCDT()`/`toggleCajita()` en Cuentas (se anota, no se borra de paso). El historial de alcancías destapadas (código vivo) no interpola texto libre del usuario, solo fechas y números.
> - **Conteo global de `onclick`:** el archivo `index.html` recibido para esta sesión ya tiene **56** atributos `onclick=` (antes eran 140 tras Cuentas/Inicio). De esos -84, los **35 documentados arriba** (27 de Plata Comprometida + 8 de Gastos) están confirmados directamente en el código fuente de cada módulo; los **49 restantes** deberían corresponder a Alcancía, pero su archivo no trae un conteo propio de "antes/después" como sí traen Gastos y Plata Comprometida — así que esa cifra es una resta, no un dato verificado línea por línea como el resto de este documento. Mismo tipo de discrepancia sin resolver que ya se anotó dos veces con el conteo de líneas de `index.html` (después de Encargos y de TC): vale la pena revisar de dónde sale antes de seguir confiando en las cifras agregadas.
> - Con estos tres, ya son **nueve módulos** con `data-action`/`Events` confirmados en el código (Spotify, Mesada, Encargos, Préstamos, Tarjetas de Crédito, Cuentas, Gastos, Plata Comprometida, Alcancía) — de los diez con archivo `.js` propio, solo Inicio se queda fuera de este conteo porque nunca tuvo `onclick` que migrar.
>
> Puntos 1, 2 y 3 de abajo actualizados con este avance.

> **Nota adicional (2026-07-24):** se recibió una copia de `index.html` de *antes* de estas tres migraciones (`antes.html`), lo que permitió cerrar el hueco que había quedado abierto el 2026-07-23 sobre el conteo de Alcancía. Contando directamente sobre ese archivo:
> - **Alcancía: 8 onclick originales** (5 en el bloque del módulo inline + 3 estáticos en `screen-alcancia`), **no ~49 como se había estimado por resta el 2026-07-23** — esa cifra era una resta sobre el conteo global y quedó explícitamente marcada como no verificada; con el archivo de antes ya es un dato contado línea por línea, igual que el resto de este documento. Los 8 coinciden exacto con las 6 acciones registradas: uno de los 8 (el botón "Cerrar" del sheet `alcancia-resultado`) ya tenía también `data-close-sheet` y no necesitó acción propia; `iniciarNueva` cubre dos sitios de llamada (pantalla "no iniciada" + sheet de resultado) con una sola acción registrada.
> - **Gastos: se reconfirmaron los 8** (6 propios del módulo + 2 estáticos del sheet `pagar-gasto-fijo`) ya documentados el 2026-07-23. Un detalle que valía la pena anotar: 2 de los 6 (los botones de estado vacío) no aparecían como `onclick="..."` literal en el código del módulo — se armaban en runtime a través del helper compartido `emptyState()`, que en `antes.html` ya soportaba tanto el string legado (`onclick="openSheet('gasto-var')"`) como el objeto `{action, args}` (`Events.attr(...)`), por venir de una migración anterior (Spotify). El barrido literal de `onclick=` en el archivo no los agarra por esta razón — mismo tipo de punto ciego que ya describía el punto 1 de este documento, aplicado ahora a un helper compartido en vez de a una función envolvente como `fuenteLabel()`.
> - **Plata Comprometida: se reconfirmaron los 27** (26 onclick + 1 onchange) exactos.
> - **Lo que sigue sin cerrar:** sumando los tres (8+8+27=43) contra la baja global de 140→56 (-84) queda un remanente de 41 que le correspondería a **Cuentas** — módulo que `antes.html` tampoco tiene migrado todavía (no aparece `cuentas.js` en sus `<script src>`), así que en teoría se podría cerrar con el mismo método. No se hizo esta vez porque el código de Cuentas no vive en un bloque contiguo en `antes.html` (está repartido entre Nu/cajitas, Nequi/Efectivo y cuentas personalizadas, en varios puntos del archivo) y aislarlo tomaría más que una sesión rápida. Además, el remanente (41) no calza con los 27 que este documento ya tenía registrados para Cuentas desde el 2026-07-22 — hay una diferencia de 14 sin explicar. Mismo tipo de inconsistencia agregada sin resolver que ya se anotó dos veces con el conteo de líneas de `index.html` (tras Encargos y tras TC): se deja anotada explícitamente en vez de forzar un cuadre.

> **Nota adicional (2026-07-25):** se migró **Configuración** (`screen-config`) a `js/modules/configuracion.js` — décimo módulo migrado. Archivo único, sin necesidad de partirse en dos: no depende de nada definido más abajo en `index.html` (mismo caso que Mesada/Gastos/Alcancía).
> - **6 `onclick` migrados, todos estáticos** (ninguno generado dinámicamente en plantillas, a diferencia de la mayoría de los módulos anteriores): botón "Salir" de la cuenta de Firebase, los dos botones "+ Agregar" de categorías (variable/fijo), el botón "Eliminar cuenta y todos los datos", y los dos accesos directos de "Herramientas" (Personas, Actividad reciente). Baja global: **56 → 50**.
> - **Mejora de paso, no pedida por el barrido de `onclick`:** los botones de eliminar categoría (`.cat-chip-del`, generados dinámicamente, uno por categoría) no usaban `onclick` — ya estaban en `addEventListener` delegado por clase, así que no sumaban al conteo de arriba — pero tampoco pasaban por `Events`. Se migraron igual a `data-action`/`Events.attr('config:eliminarCat', tipo, cat)` para que la pantalla completa quede bajo un único mecanismo de despacho, en vez de dos (`Events` + un delegado propio aparte).
> - **`.innerHTML` sin escapar: no se encontró ningún caso en código vivo** — igual que Alcancía. Los chips de categoría y los `toast()` de agregar/eliminar ya usaban `escHtml()` en todos los sitios. Segunda vez (de diez módulos) que no se repite este hallazgo.
> - **Funciones sacadas de tres bloques `<script>` compartidos distintos, no de uno solo — mismo patrón ya visto en TC/Cuentas/Inicio:** categorías + backup JSON venían del bloque núcleo grande (junto a `S`, `save`, `escHtml`, etc.); `toggleModulo`/`borrarTodo` de otro bloque más abajo; y `exportarCSV` compartía `<script>` con "Búsqueda global"/Presupuestos (secciones numeradas 1-9 de un IIFE, sin relación real con Configuración — la misma IIFE de la que ya se habían sacado antes las secciones de salud financiera/proyección al migrar Inicio). Se dejaron a propósito en `index.html`: `applyModulos()` (toca Cuentas/Mesada/Inicio, no solo esta pantalla — mismo criterio que `navTo()`/`refresh()`), todo el auth de Firebase (`_fbSignOut`, `_abrirEliminarCuenta`, `_fbDeleteAccount` — núcleo de sesión, no exclusivo de esta pantalla) y el gate de PIN/biometría completo (se muestra al abrir la app, no solo desde Configuración). `getCatsVar()`/`getCatsFijo()`/`CATS_VAR_DEFAULT`/`CATS_FIJO_DEFAULT` tampoco se movieron: las comparte Gastos.
> - **Detalle de orden de carga verificado, no un problema:** `leerArchivoImport()` tiene un override posterior en `index.html` ("MEJORA 5: Validación") que la reemplaza por una versión con validación de estructura del JSON — mismo patrón ya usado con `addGastoVar` en Gastos. Sigue funcionando igual porque `configuracion.js` carga antes que ese bloque de overrides; no se tocó ninguno de los dos lados.
> - `index.html` bajó de 10.934 a 10.758 líneas (-176).
>
> Puntos 1, 2 y 3 de abajo actualizados con este avance.

> **Nota adicional (2026-07-26):** se migró **Actividad reciente** (el Feed de actividad financiera, `screen-historial`) a `js/modules/actividad_reciente.js` — undécimo módulo migrado. Archivo único, sin dependencia real de orden de carga hacia funciones definidas más abajo (mismo caso que Mesada/Gastos/Alcancía/Configuración); el único punto sensible al orden es el wrap de `window.refresh()` que hace al cargar, que exige seguir cargando el archivo en la misma posición del documento donde vivía el IIFE original (después de que `refresh()` ya esté definido).
> - **`onclick`: nada que migrar** — pantalla de solo lectura, sin ningún `onclick` propio en el módulo ni en `screen-historial`. Mismo caso que Inicio: no aporta al conteo de `onclick` restantes, la ausencia de avance en esta cifra no significa que se saltó trabajo.
> - **Hallazgo fuera del alcance de `onclick`/`data-action`:** el módulo tenía su propio `document.addEventListener('click', ...)` delegado (para refrescar el feed al entrar a la pantalla) — justo el tipo de duplicación que `events.js` dice evitar en su propio comentario de cabecera. No se convirtió a `Events.on()` porque no es una acción puntual con handler+argumentos sino un observador de navegación sobre varios selectores; se dejó como listener aparte, pero se aprovechó para limpiarlo: de los tres selectores que escuchaba (`[data-screen="historial"]`, `#mas-historial`, `#cfg-historial-row`), los dos primeros apuntaban a elementos que no existen en el HTML recibido esta sesión — ningún ítem del nav inferior usa `data-screen="historial"` y el menú "Más" no tiene entrada "Actividad reciente" — y nunca se disparaban. Se anota en vez de borrarse en silencio, mismo criterio que `toggleCDT()`/`toggleCajita()` en Cuentas.
> - **`.innerHTML`: sin hallazgos** — tercera vez (de once módulos) que sale limpio, junto con Alcancía y Configuración. Todo el texto libre (`item.titulo`, `item.subtitulo` de las siete fuentes normalizadas) confluye en un único punto de render que ya lo pasa por `esc()`.
> - `index.html` bajó de 10.934 a 10.542 líneas (-392, la baja más grande vista hasta ahora en relación al tamaño del bloque movido, porque el IIFE traía inline los SVG de los diez íconos del feed).
> - **Discrepancia encontrada, sin resolver:** el `index.html` recibido esta sesión **no tiene `configuracion.js` cargado** (ningún `<script src="js/modules/configuracion.js">`) y `#cfg-historial-row`/el botón "Personas" de Configuración siguen con su `onclick` inline sin tocar — pese a que este documento y `CHANGELOG.md` ya registran esa migración como hecha (nota del 2026-07-25). Mismo tipo de inconsistencia documento-vs-código ya anotada varias veces antes (Préstamos el 2026-07-20; Gastos/Plata Comprometida/Alcancía el 2026-07-23; el conteo de líneas tras Encargos y tras TC) — no se investiga ni se revierte acá, porque el trabajo de Configuración (si hace falta rehacerlo) no es el alcance de esta sesión. Queda anotado para la próxima vez que se toque esa pantalla: verificar contra el código real, no contra este documento, antes de asumir que ya está hecho.
>
> Puntos 1 y 3 de abajo actualizados con este avance.

> **Nota adicional (2026-07-27):** esta sesión no migró módulos nuevos — encontró **dos migraciones más ya hechas que este documento nunca había registrado**: `js/modules/analisis.js` (Ingresos Fijos + Presupuestos) y `js/modules/personas.js` ya existen, ya están cargados desde `index.html`, y ya tienen `data-action` propio (`analisis:...`, `personas:...`). Mismo caso ya anotado tres veces antes (Préstamos el 2026-07-20; Gastos/Plata Comprometida/Alcancía el 2026-07-23) — el trabajo se hace, el documento se entera después.
> - **Diferencia con las veces anteriores: no se recibió el archivo de ninguno de los dos módulos, solo `index.html`.** Todo lo de abajo está verificado contra lo que `index.html` deja ver (el `<script src>`, los `data-action` que quedan estáticos en el documento, y los comentarios de migración dejados en el propio archivo) — no contra el código fuente de `analisis.js`/`personas.js` línea por línea, como sí se pudo hacer con los ocho módulos anteriores. Los detalles de `.innerHTML` sin escapar, código muerto, o el desglose exacto de `onclick` migrados dentro de cada archivo quedan **sin verificar** hasta que se audite con esos archivos a la vista.
> - **Análisis:** cubre Ingresos Fijos y Presupuestos (antes tres bloques sueltos en `index.html`, incluyendo un IIFE numerado por secciones que ya se había visto al migrar Inicio y Configuración — mismo patrón de "código no relacionado compartiendo bloque"). En `index.html` solo quedan **2 `data-action="analisis:..."` estáticos** (`abrirSheetIngresoFijo`, `abrirPresupuestos`) — el resto de la UI de Análisis (si tiene más `onclick`/acciones) se genera dentro del propio módulo y no es visible desde acá.
> - **Personas:** **1 `data-action="personas:..."` estático** visible en `index.html`. El archivo ya trae su propia doc (`personas.md`, referenciada en el comentario de migración) — no se leyó esta sesión.
> - **Hallazgo en el propio `index.html`, corregido:** el comentario de la CSP (cerca de la línea 13) todavía decía *"existan los ~247 atributos onclick=... inline"* como justificación de mantener `'unsafe-inline'` — esa cifra era la de antes de Spotify, la primera migración de todas. El archivo real de esta sesión tiene **24** `onclick=` inline, y los 24 son de PIN/biometría/login (`_pinKey`, `_pinDel`, `_pinBioTrigger`, `_pinOlvide`, `_fbSignIn`, `_fbDeleteAccount`, `_pinSetNew`, `_pinDisable`, `_bioSetup`, `_bioDisable`) — núcleo de sesión, no de ningún módulo de negocio. Es decir: **ya no queda ningún `onclick` de módulo por migrar** en este archivo; lo único pendiente es el gate de autenticación, que nunca estuvo en el alcance de la migración por módulos (ver punto 1 más abajo). Comentario actualizado directamente en `index.html` esta sesión (24 en vez de ~247, con la aclaración de que son del gate de auth).
> - **Conteo global:** 24 `onclick`, contra los 50 que este documento tenía registrados tras Configuración (2026-07-25) y sin pasar por la cifra de Actividad Reciente (que no aportaba al conteo). La baja de 50→24 (-26) coincidiría con que Análisis y Personas se llevaron el resto de lo que faltaba — pero, igual que con Cuentas el 2026-07-24, **es una resta, no un dato verificado línea por línea** sin los archivos de los módulos. Se deja como hallazgo abierto en vez de forzar el cuadre.
> - Con estos dos, quedan **trece módulos** confirmados con `.js` propio y `data-action` (Spotify, Mesada, Encargos, Préstamos, Tarjetas de Crédito, Cuentas, Inicio, Actividad Reciente, Gastos, Plata Comprometida, Alcancía, Configuración, Análisis) más Personas — de los cuales solo Préstamos sigue sin su nota de detalle propia (pendiente desde 2026-07-20).
>
> Puntos 1 y 3 de abajo actualizados con este avance.

> **Nota adicional (2026-07-28):** se recibieron por fin `analisis.js` y `personas.js`, lo que permite cerrar el hueco dejado el 2026-07-27 ("sin verificar hasta que se audite con esos archivos a la vista"). Verificado línea por línea contra el código fuente real:
> - **Análisis:** 4 acciones (`abrirSheetIngresoFijo`, `abrirPresupuestos` estáticas en `index.html`; `editarIngresoFijo`, `eliminarIngresoFijo` dinámicas dentro del módulo) — coincide con lo estimado el 27 sin corregir nada. **`.innerHTML` sin escapar encontrado y corregido:** `cat` (nombre de categoría, texto libre — el usuario las agrega desde Configuración con `config:agregarCat`) se interpolaba sin pasar por `escHtml()` en **5 sitios**: la card de "Top categorías del mes", el `<label>` y el atributo `data-cat` del formulario de Presupuestos, la barra de progreso de Presupuestos, y el mensaje del `toast()` de aviso al 80% del presupuesto. Es el mismo patrón que ya se repitió doce veces en este documento — pero esta vez con un campo nuevo (`cat`, no `fuenteLabel()`/`nombre`/`nota` como en los módulos anteriores). Corregido envolviendo las 5 interpolaciones en `escHtml()`.
> - **Personas: sale limpio de `.innerHTML`** — todo el texto libre que maneja (`p.nombre`, `d.nombre`, `nota` de movimientos, término de búsqueda `q`, iniciales) ya pasa por `escHtml()` en los ~15 sitios donde se interpola, incluyendo los tres perfiles distintos que arma (persona, deudor, "le debo"). Tercera vez (de trece módulos) que este hallazgo no aparece, junto con Alcancía y Configuración.
> - **Corrección importante al "ya no queda ningún `onclick` de módulo" del 2026-07-27: no es exacto.** `personas.js` tiene un `onclick="_irAEncargo('${e.id}')"` inline en el botón "Ver →" de cada encargo dentro del perfil de persona — generado dinámicamente por el módulo, así que **nunca apareció en el grep de `index.html`** que sostenía la cifra de 24. Está autodocumentado en el propio archivo (comentario junto al registro de eventos): sigue sin migrar a propósito, porque `_irAEncargo()` vive en `encargos-personas.js` y migrarlo requiere registrar `'encargos-personas:irAEncargo'` desde ese archivo — no se tocó, ese archivo no se recibió esta sesión.
> - **Hallazgo nuevo, mismo tipo de punto ciego que ya se describía para `onmouseenter`/`onmouseleave`:** `personas.js` tiene un `oninput="_selPersonaFiltrar()"` inline en el buscador del sheet "¿De quién es la plata?" — primer caso de `oninput` encontrado en todo el código, nunca contado en ninguna cifra de este documento (que solo trackea `onclick`).
> - **Confirmación concreta del hallazgo de `onmouseenter`/`onmouseleave`** que el punto 1 ya mencionaba en abstracto ("~10 en toda la app"): `personas.js` trae **3 pares** (persona, deudor, "le debo" en sus respectivas filas de lista) con el mismo patrón de hover inline — mismo caso, sigue sin tocarse por el mismo motivo (mejor resolverlo con una clase CSS `:hover` compartida, no módulo por módulo).
> - **No hay código muerto** en ninguno de los dos archivos (sin bloques `if(false)` ni funciones sin uso).
> - `analisis.js` corregido esta sesión (los 5 sitios de `${cat}` envueltos en `escHtml()`). `personas.js` no se tocó — no tenía nada que corregir en `.innerHTML`, y el `onclick`/`oninput` que le quedan están fuera del alcance de esta sesión (dependen de `encargos-personas.js`, no recibido, o son parte del hallazgo agregado de hover ya descrito).
>
> Puntos 1 y 2 de abajo actualizados con este avance.

> **Nota adicional (2026-07-29):** se recibió `encargos-personas.js`, lo que permite cerrar el último `onclick` de negocio que quedaba pendiente (ver nota 2026-07-28). **Corregido en esta sesión, no solo documentado:**
> - `personas.js`: el botón "Ver →" de cada encargo en el perfil de persona pasó de `onclick="_irAEncargo('${e.id}')"` a `${Events.attr('encargos-personas:irAEncargo', e.id)}`.
> - `encargos-personas.js`: se agregó `Events.on('encargos-personas:irAEncargo', _irAEncargo);` junto a la función, siguiendo el mismo patrón que el resto del archivo (`Events.on('encargos:seleccionarPersonaNueva', ...)`, ya existente). El orden de carga no es un problema: `Events.attr()` solo arma el atributo `data-action` en el momento de renderizar el perfil (después de que todos los `<script>` ya cargaron), y `personas.js` carga antes que `encargos-personas.js` en `index.html` — mismo orden que ya usan Spotify/Encargos con sus archivos de integración con Personas.
> - **Con esto, el conteo de `onclick` de negocio pendiente vuelve a 0** (dato verificado contra los tres archivos involucrados, no una resta).
> - **`.innerHTML`: sin hallazgos** en `encargos-personas.js` — el único texto libre que toca (`p.nombre`) se asigna a propiedades DOM (`.title`, `.textContent` vía `iniciales()` en el módulo de Personas) o dentro de un `.innerHTML` que solo trae SVG fijo, nunca el nombre interpolado directo en HTML. Cuarta vez (de trece archivos de integración/módulo revisados a este nivel) que no aparece.
> - **Hallazgo de estilo, no de seguridad:** `encargos-personas.js` engancha sus dos botones de "Ver perfil" (avatar de la card, chip del detalle) con `.onclick = () => ...` y `addEventListener('click', ...)` en vez de `data-action`/`Events`. No es un problema de CSP (son asignaciones desde JS, no atributos inline en el HTML — `'unsafe-inline'` no los afecta), pero es inconsistente con el mecanismo único de despacho que ya usa el resto de la app. No se tocó por ser de bajo impacto.
> - **Hallazgo nuevo, sin investigar:** el comentario de cabecera del archivo menciona que la integración de "Deudores" con Personas **todavía no se migró** (a diferencia de Préstamos/`prestado.js`, que este documento ya registra como migrado desde el 2026-07-20) y referencia un documento nuevo, `plan-migracion-personas.md`, que no se ha visto en esta auditoría. No está claro si "Deudores" es distinto de `prestado-personas.js` (que sí está cargado en `index.html`) o es el mismo trabajo con otro nombre — se deja anotado sin forzar la reconciliación, mismo criterio que otras discrepancias de este documento.
>
> Puntos 1 y 3 de abajo actualizados con este avance.

> **Nota adicional (2026-07-30):** se recibieron `prestado.js` y `prestado.md`, lo que cierra la nota de detalle de Préstamos pendiente desde el 2026-07-20 — y resuelve la duda sobre "Deudores" dejada abierta el 2026-07-29.
> - **"Deudores" SÍ está migrado — la duda anterior era por un comentario desactualizado, no por trabajo faltante.** `prestado.js` cubre "Me deben" (`S.deudores`) completo bajo el namespace `data-action="prestado:..."`: `abrirDeudor`, `abrirPerfilDeudor`, `abrirPerfilDeudorActual`, `eliminarDeudorActual`, `editarDeudorActual`, `addDeudor`, `abrirSheetNuevaPersona`, `eliminarMovDeudor`, `abrirDetalleMov`, entre otros — junto con "Yo debo" (`S.misDeudas`) y "Préstamo con TC". El comentario de cabecera de `encargos-personas.js` que decía "Deudores todavía no se migró" estaba desactualizado; se corrigió esta sesión para reflejar que sí está migrado y en qué archivo. `plan-migracion-personas.md` sigue sin verse — puede que ya no aplique, o puede tener contexto de un momento anterior a esta migración; no se investiga más sin el archivo.
> - **Conteo de `onclick`:** 24 en total confirmados por el propio módulo (6 estáticos en `index.html`, 18 dinámicos en los renders de listas/historial), migrados a `data-action`. Los `onclick="event.stopPropagation()"` que se usaban para no burbujear clicks en filas anidadas se resuelven con `data-stop-click="true"` + un solo `addEventListener` centralizado — verificado, no quedan como atributo inline.
> - **Hallazgo nuevo, no contado en ninguna cifra de este documento: 9 `onchange`/`oninput` inline sin migrar**, todos en las filas dinámicas de "dividir entre cuentas" (2 en split de Préstamo, 2 en split de Abono, 5 en la sección "Extra" del abono) — pasan índice de array y `this.value` directo a funciones globales (`abonoSplitFuente`, `extSetMonto`, etc.). Mismo tipo de punto ciego que el `oninput` ya encontrado en Personas, pero acá son 9 de una sola vez, todos generados dinámicamente así que tampoco aparecen en el grep de `index.html`. No se migran esta sesión: requeriría reestructurar las funciones de render de filas para enganchar `addEventListener` después de insertar cada fila (como ya se hace con `data-stop-click`), en vez de un cambio de una línea — más riesgo que las correcciones puntuales de escapado.
> - **`.innerHTML`: dos sitios sin escapar, corregidos.** El módulo es cuidadoso con `escHtml()` en general (49 usos, incluyendo la mayoría de los `toast()` con nombre de persona) — pero **los dos toasts de "recién creado" se quedaron sin envolver**: `` `${nombre} agregado/a` `` en `addDeudor()` y `` `Deuda con ${nombre} agregada` `` en `crearMiDeuda()`, ambos con el nombre tal cual lo escribió el usuario en el sheet de alta. Los toasts de eliminar, de error, y el de advertencia de saldo (línea 175) sí ya escapaban el nombre correctamente — el patrón se rompió justo en las dos confirmaciones de creación. Corregido envolviendo ambos en `escHtml()`. Es el mismo tipo de hallazgo que ya se repitió con Análisis (`cat`) y otros ocho módulos antes — confirma otra vez que hay que revisar cada sitio de salida, no asumir que un módulo "cuidadoso" cubre el 100% de los suyos.
> - **No hay código muerto** en el archivo.
>
> Puntos 1 y 3 de abajo actualizados con este avance.

> **Nota adicional (2026-07-31):** se recibió `configuracion.js`, lo que permite reconfirmar la migración de Configuración contra su código fuente real y cerrar la discrepancia dejada abierta el 2026-07-26 ("el `index.html` recibido esa sesión no tenía `configuracion.js` cargado").
> - **Discrepancia resuelta: sí es cierto que Configuración está migrada, tal como se registró el 2026-07-25.** El `index.html` de esta sesión sí tiene `configuracion.js` cargado, y sus 6 `data-action="config:..."` estáticos aparecen todos donde se esperaba (`signOut`, `agregarCat` ×2 — var y fijo —, `abrirEliminarCuenta`, `irA` ×2 — personas e historial). La discrepancia del 2026-07-26 quedó como un artefacto puntual de esa sesión (un `index.html` desactualizado o de una rama distinta) — no como un problema real de la migración. No hay forma de confirmar qué la causó sin ese archivo de esa sesión, así que se deja sin más investigación, igual que otras discrepancias puntuales de este documento.
> - **Conteo de `onclick`:** 6 estáticos + 1 dinámico (`eliminarCat` en el chip de categoría) = 7, no 6 — el dinámico no se había contado antes porque el chip de eliminar categoría vive en una plantilla, no como atributo estático en `index.html`. No cambia ninguna conclusión (sigue sin texto libre de por medio, solo `tipo`/`cat` como argumentos), pero corrige la cifra.
> - **`.innerHTML`: sigue limpio**, confirmando lo ya anotado el 2026-07-25 — los 4 usos de `escHtml()` del archivo cubren los 4 sitios donde se interpola el nombre libre de categoría (el chip y los 3 `toast()` de agregar/eliminar). Cuarta vez (de trece módulos) que este hallazgo no aparece, junto con Alcancía, Configuración (ya contaba, se reconfirma) y Personas.
> - **Hallazgo confirmado con caso concreto: 1 par `onmouseenter`/`onmouseleave`** en el botón de eliminar categoría del chip — mismo patrón ya estimado en el punto 1, ahora con un caso más localizado (van 4 pares confirmados de los ~13 estimados: 3 en Personas, 1 acá).
> - **No hay código muerto.** El wiring de `leerArchivoImport` a través de un wrapper `(e) => leerArchivoImport(e)` (para que el override de "MEJORA 5" más abajo en `index.html` siga tomando efecto) ya está en el archivo, consistente con la corrección de ese bug ya registrada en `CHANGELOG.md`.
>
> Con esta nota, los trece módulos con `.js` propio quedan con su detalle confirmado contra código fuente real. No queda ningún módulo del inventario original sin auditar a este nivel.

> **Nota adicional (2026-08-01):** se resolvió el hallazgo dejado abierto el 2026-07-29 sobre "Deudores" — la duda de esa fecha ("no está claro si 'Deudores' es distinto de `prestado-personas.js` o es el mismo trabajo con otro nombre") apuntaba a algo real, solo que a la parte equivocada. La **CRUD de deudores** (`S.deudores`) sí está migrada en `prestado.js`, tal como se confirmó el 2026-07-30 — pero el **selector de persona compartido** de "Agregar persona" (Me deben) / "Nueva deuda" (Yo debo) — `_onSelPersonaMeDeben`, `_initNuevaDeudaPersonaSelector`, `_onSelPersonaNuevaDeuda`, más los hooks de `openSheet`/`crearMiDeuda` que lo conectan — seguía viviendo inline en `index.html` en un bloque `<script>` sin migrar, justo después de `<script src="js/modules/prestado-personas.js">`, a diferencia de Spotify/Encargos/Préstamos (que sí tienen su propio archivo `-personas.js`).
> - Se extrajo a **`js/modules/deudores-personas.js`** — nuevo archivo, mismo motivo de carga que `prestado-personas.js`/`spotify-personas.js`/`encargos-personas.js` (depende de `getPersona`/`abrirSelPersona`/`_inyectarPersonaSheets`, recién definidas en ese punto del documento) más un motivo adicional propio: envuelve `openSheet` y `crearMiDeuda`, ambos ya envueltos una vez por `prestado-personas.js`, así que necesita cargar después de ese archivo.
> - Código movido tal cual, sin reescritura — no se repite la auditoría de `.innerHTML` porque no se tocó ninguna interpolación.
> - **Se dejó a propósito inline en `index.html`:** el fallback `window.addEventListener('appDataLoaded', ...)` que llama a `_inyectarPersonaSheets()` al cargar la app. No es específico de Deudores — es bootstrap del sistema de Personas completo, compartido por Spotify/Encargos/Préstamos/Deudores por igual — mismo criterio ya usado con `refresh()`/`navTo()`: estar físicamente cerca del código que sí se migra no lo vuelve parte de ese módulo.
> - `index.html` bajó de 9.252 a 9.123 líneas (-129).
> - **Comentario de cabecera de `prestado.js` corregido de paso** — solo mencionaba `prestado-personas.js`; se agregó la referencia al nuevo `deudores-personas.js` para que no quede desactualizado desde el día uno, mismo tipo de hallazgo que este documento ya señaló con `encargos-personas.js` (nota del 2026-07-30) y con el comentario de la CSP (nota del 2026-07-27).
> - Con esto, Préstamos queda dividido en tres archivos (`prestado.js`, `prestado-personas.js`, `deudores-personas.js`) en vez de dos — a diferencia de Spotify/Encargos, que solo necesitaron partirse en dos. `plan-migracion-personas.md` sigue sin aparecer — no se investiga más sin el archivo, mismo criterio que el 2026-07-30.
>
> Punto 3 de abajo actualizado con este avance.

> **Nota adicional (2026-08-02):** se cerraron los tres hallazgos de `onchange`/`oninput`/hover inline que el punto 1 traía documentados desde el 2026-07-28/30/31 (`personas.js`, `prestado.js`, `configuracion.js` recibidos esta sesión, más `index.html`).
> - **Préstamos: los 9 `onchange`/`oninput` de filas dinámicas migrados.** Los tres puntos donde vivían (split de origen del préstamo, split de destino del abono, sección "Extra" del abono) pasaron de `onchange="fn(${i},this.value)"`/`oninput="..."` a una clase `data-i="${i}"` por campo + `querySelectorAll(...).forEach(el => el.addEventListener(...))` inmediatamente después de cada `innerHTML =`, reutilizando las funciones globales ya existentes (`abonoSplitFuente`, `extSetMonto`, etc.) — mismo patrón que el archivo ya usaba para `data-stop-click` en la sección Extra, no infraestructura nueva.
> - **Personas: el `oninput` del buscador y los 3 pares `onmouseenter`/`onmouseleave` migrados.** El buscador (`#sel-persona-buscar`) se wireó una sola vez con `addEventListener('input', ...)` justo después de inyectar el sheet (`_inyectarPersonaSheets()`, que ya tiene guard contra doble inyección). Los tres pares de hover (persona, deudor sin perfil, "le debo" sin perfil) no se pudieron resolver con un `:hover` de CSS porque el color varía por fila — se resolvieron con `data-hover-color="${color}"` + `addEventListener('mouseenter'/'mouseleave', ...)` tras cada render, mismo patrón que el archivo ya usaba en `_selPersonaFiltrar()` (líneas 340-343 antes de esta sesión) para el hover de la lista de selección.
> - **Configuración: el par restante sí fue un `:hover` de CSS puro** (a diferencia de Personas) — a diferencia del hover de las filas de Personas, los colores del botón de eliminar categoría son fijos (`var(--red)`/`var(--text3)`), así que se resolvió con una regla `.cat-chip-del:hover{color:var(--red);}` agregada al `<style>` de `index.html`, sin JS. Van 4 de 4 pares confirmados con ubicación exacta resueltos.
> - **De paso, en `index.html`:** se resolvieron también los 2 pares `onmouseenter`/`onmouseleave` de las tarjetas "Meta"/"CDTs" del detalle de cajita (Cuentas) — no contaban en la cifra de ~13 de este documento (nunca se habían confirmado con ubicación exacta), pero vivían directo en `index.html` y sus colores son fijos, así que se resolvieron igual que Configuración, con reglas `:hover` en el mismo bloque de `<style>`.
> - **`node --check` sin errores** en `personas.js`, `prestado.js` y `configuracion.js` tras los cambios. No se tocó ningún `Events.attr`/`data-action` existente.
> - **Lo que sigue sin cerrar de este punto:** los ~9 pares `onmouseenter`/`onmouseleave` restantes de los ~13 estimados originalmente (ubicación no confirmada — probablemente repartidos en otros módulos no revisados a este nivel, ej. los "2-3 en los botones de eliminar compra/pago de Tarjetas de Crédito" que la primera nota de este punto ya mencionaba sin confirmar) y los **24 `onclick` del gate de PIN/biometría/login**, que siguen siendo una decisión pendiente y no un olvido — migrarlos es lo único que permitiría quitar `'unsafe-inline'` de la CSP del todo.
>
> Punto 1 de abajo actualizado con este avance.

> **Nota adicional (esta sesión):** se verificó directamente contra `index.html` (no por resta) el estado que el punto 1 dejaba abierto el 2026-08-02, y se cerró por completo: los ~9 pares de hover "sin ubicar" resultaron ser 0 (no había ningún atributo `onmouseenter`/`onmouseleave` inline, solo dos comentarios que documentaban que ya se habían resuelto con CSS), y los "24 onclick" del gate de PIN/biometría/login eran en realidad 21 (nunca se habían contado línea por línea contra el archivo). Los 21 se migraron a `data-action`/`Events` esta sesión (namespaces nuevos `pin:` y `authgate:`), cerrando el punto 1 del todo. Detalle completo de qué se hizo y por qué en `CHANGELOG.md#infraestructura--seguridad`, según la convención de este documento. Al revisar si esto ya permitía sacar `'unsafe-inline'` de la CSP, apareció un hallazgo nuevo, todavía sin resolver — ver el punto 1 reescrito abajo.

> **Nota adicional (sesión posterior):** primer avance real del punto 2 de la priorización (CSP, ver punto 1 de "Críticos" reescrito arriba). De los 18 bloques `<script>` inline que quedaban: 6 no tenían código real (solo comentarios de migraciones viejas, convertidos a comentarios HTML) y 9 eran autocontenidos, externalizados a `js/core/` — `firebase-init.js` y `pin-bio.js` (`type="module"`, WebAuthn), `firebase-sync.js` (`type="module"`, guardado/carga con Firestore), `mejoras.js` y `mejoras-adicionales.js` (ocultar saldos, hook de `refresh()`, Service Worker, autofocus, aria-labels), `nav.js` (`navTo()`), `bootstrap.js` (`iniciales()`, autosave), `personas-init.js` e `import-validado.js`. Quedan **3 bloques inline** (antes ~18): el núcleo `S`/`save()`, el sheet-stack (`openSheet`/`closeSheet`) y `refresh()`/menú "Más" — deliberadamente no tocados, ver el punto 1 de "Críticos" para el porqué. Verificado con `node --check` (`--input-type=module` en los tres `type="module"`) en cada archivo nuevo, y re-verificados los 3 bloques que quedaron inline extrayéndolos por rango de línea exacto (un regex ingenuo sobre `<script>...</script>` da falsos positivos cuando un comentario menciona la palabra "`<script`" como texto — pasó al intentarlo esta misma sesión). Conteo final verificado con un parser HTML real: 35 tags `<script>`, 32 con `src`, 3 inline. `index.html` bajó de 8.219 a 6.963 líneas (-1.256, la mayor caída de una sola sesión hasta ahora). Detalle completo en `CHANGELOG.md#infraestructura--seguridad`.
>
> **Nota de verificación actualizada, reemplaza la del punto 4 de abajo (las líneas que citaba ya no aplican tras esta extracción):** `S`/`save()` ahora vive en el primer bloque `<script>` inline que queda (antes ~4770-5724, hay que recontar la línea exacta contra este `index.html`), el sheet-stack (`openSheet`/`closeSheet`) en el segundo, y `refresh()` + menú "Más" en el tercero — mismo contenido, sin reordenar ni reescribir nada, solo desplazados por las líneas que se sacaron alrededor. La cadena de tres reasignaciones de `window.refresh` documentada abajo (antes en líneas 6474/7282/7771) sigue existiendo igual, solo que dos de sus tres puntos (los que estaban en los bloques "MEJORAS" y "MEJORAS ADICIONALES") ahora viven en `js/core/mejoras.js` y ya no en `index.html` — el wrap sigue funcionando porque se cargan en la misma posición relativa de antes, pero vale la pena tenerlo en cuenta la próxima vez que se busque esa cadena por número de línea de `index.html`: dos de los tres eslabones ya no están ahí.
>
> Punto 1 de abajo reescrito con este cierre.

> **Nota adicional (sesión posterior):** al retomar el punto 3 (redistribución de `_initEventListeners()`, ver abajo) con `encargos.js`/`prestado.js`/`tarjetas_credito.js` reales en mano, apareció una discrepancia real entre este documento/`CHANGELOG.md` y el código: la nota del 2026-07-27 ("Redistribución final: Encargos y Préstamos") documentaba seis listeners como movidos a `encargos.js` (`movenc_monto`, `movenc_mia_cuenta_sale`/`_entra`, `ctc_monto`, `ctc_cuenta_enc`/`ctc_tarjeta`/`ctc_destino`, y los tres "valor real" del motor Diferencial), pero **ese bloque nunca llegó al archivo** — se verificó con `node --check` y jsdom que el archivo real no tiene ningún `addEventListener` sobre esos seis campos, a diferencia de `prestado.js`, donde el bloque equivalente (`mov_desde_encargo`, `mov_enc_sel`, `mov_enc_cuenta`, `mov_tiene_extra`, `mov_extra_monto`, `prtc_dif_real`) sí está y funciona. Efecto real confirmado con jsdom contra el archivo original: cambiar la cuenta "sale"/"entra" en "Retirar plata" no actualizaba el preview en vivo; mismo problema en "Compra con TC" y en los tres inputs de "valor real" del Diferencial. Se corrigió agregando a `encargos.js` el mismo bloque `forEach` que ya usa `prestado.js` — sin funciones nuevas, solo el wiring que faltaba. Detalle completo en `CHANGELOG.md`, sección "Núcleo compartido — cierre de `_initEventListeners()`". Con esto, la redistribución de Encargos + Préstamos queda cerrada de verdad — no solo documentada.

---

## 🔴 Críticos — pendientes

### 1. CSP: los bloques `<script>` inline exigían `'unsafe-inline'` (✅ cerrado definitivamente — 29 archivos auditados, `'unsafe-inline'` fuera de `script-src`)

**Estado:** con la migración de `onclick` ya cerrada del todo (ver `CHANGELOG.md#infraestructura--seguridad`, 0 atributos `onclick`/`onchange`/`oninput`/hover inline en `index.html`), se revisó si eso ya permitía sacar `'unsafe-inline'` de `script-src` en la CSP, como asumía el comentario del propio archivo — **no es así**. `'unsafe-inline'` en `script-src` no solo habilita los atributos `onclick="..."` inline: también habilita cualquier bloque `<script>` inline (sin `nonce`/`hash`).

**Avance real (esta sesión):** se descartó `nonce` de plano — GitHub Pages sirve archivos estáticos, sin servidor que genere un valor aleatorio por request y lo mande en una cabecera HTTP real, así que un nonce fijo pegado en el HTML no protege nada. También se descartó hash (`'sha256-...'`) como estrategia general: viable para contenido que no cambia, pero cualquier edición futura a un bloque invalida su hash — inaceptable para código que se sigue tocando activamente. Se optó por la única opción sin ese riesgo: seguir el mismo patrón que los módulos, externalizar a `.js` con `<script src>` (cubierto por `'self'`). Resultado: de los 18 bloques `<script>` inline que había, **6 no tenían código real** (solo comentarios de migraciones viejas — convertidos a comentarios HTML) y **9 eran autocontenidos** (sin dependencia de orden de carga con el núcleo) — externalizados a `js/core/`.

**Corrección posterior, misma sesión (bug real en producción):** al probar el login, `firebase-init.js` y `firebase-sync.js` resultaron tener un problema que "autocontenido" no capturaba: no dependen del núcleo, pero sí están en el camino crítico *antes* de que exista cualquier pantalla de carga que proteja al usuario. `firebase-init.js` muestra el botón de login apenas termina de ejecutarse; `Events.registerAll('authgate', ...)` (el handler de ese botón) vivía en `firebase-sync.js`, el siguiente `<script type="module">` del documento. Inline, los dos ya venían completos en el HTML, sin fetch de por medio. Externalizados, cada uno pasa a depender de su propia descarga — y con red lenta, el botón podía quedar visible antes de que su handler se registrara. Se revirtieron esos 2 a inline (el mismo patrón defensivo que el código ya usaba para el gate de PIN, un `setTimeout` de 5s de seguridad, no existía para `authgate` porque nunca lo había necesitado). Quedaron **7 archivos externalizados** (`pin-bio.js`, `mejoras.js`, `mejoras-adicionales.js`, `nav.js`, `bootstrap.js`, `personas-init.js`, `import-validado.js`) y **5 bloques inline** (los 3 del núcleo + estos 2).

> **⚠️ Esta hipótesis de carrera de red quedó descartada** (ver la nota siguiente) — el bug real no tenía nada que ver con timing ni con esta sesión. Se deja constancia igual: revertir esos 2 archivos a inline sigue siendo una precaución razonable de todos modos (ahora que el bug real está corregido y el registro sí llega a ejecutarse, una carrera de red genuina volvería a ser posible si se re-externalizan sin agregar antes algún guard de "todavía no está listo").

**Corrección real (sesión posterior, tras persistir el bug en modo incógnito):** modo incógnito descarta caché/Service Worker — y comparar el bloque reinsertado contra el `index.html` original (antes de tocar nada) dio **idéntico byte a byte**. El bug ya estaba en el archivo que subió el usuario, sin relación con esta auditoría ni con la extracción de scripts. Causa real: `events.js` declara `const Events = (function(){...})();` a nivel superior de un script clásico — eso crea una variable global *léxica* (`Events`, accesible a secas desde cualquier script, incluidos los `type="module"`), pero **nunca** se cuelga como propiedad de `window`. El guard de `authgate` (y, clonado, el de `pin` en `pin-bio.js`) chequeaba `window.Events` — que es `undefined` para siempre, sin importar el orden de carga — así que el registro nunca se ejecutaba, en ningún escenario, ni con red lenta ni rápida. Alcance real: no solo el login — el teclado de PIN y biometría tampoco tenían ningún handler registrado. Fix: `window.Events` → `typeof Events !== 'undefined'` en los dos puntos. Detalle completo (incluida la nota honesta de que no se pudo probar con navegador real en este entorno, sin acceso de red para instalar uno) en `CHANGELOG.md#infraestructura--seguridad`.

**Lo que queda (3 bloques núcleo, sin contar particiones internas):** `S`/`save()` (955 líneas), sheet-stack/nav (originalmente etiquetado solo "sheet-stack" — el mapeo mostró que también incluye `showScreen`, `applyModulos`, wiring legacy de eventos y overrides de `addGastoVar`/`addGastoFijo`/`addSpotify`; ver más abajo) y `refresh()` + menú "Más" (389 líneas). `firebase-init.js`/`firebase-sync.js` ya no están en esta lista: se les agregó un guard explícito (botón de login `disabled` hasta confirmar el registro de `authgate`, con timeout de seguridad de 8s) y se volvieron a externalizar — detalle en `CHANGELOG.md#infraestructura--seguridad`. Moverlos de verdad requiere la reestructuración de los puntos 3/4 de abajo, no un cambio quirúrgico aislado.

> **Mapeo del bloque sheet-stack/nav (sesión posterior):** no es un solo sistema — son al menos 4 piezas con distinto riesgo: (1) auto-formateo de inputs de plata, autocontenido salvo por `_moneyDigits`/`_moneyRender` (viven en el bloque `S`/`save`, que carga antes) — **ya extraído** a `js/core/money-input.js`; (2) sheet-stack + `showScreen` + `applyModulos`, con fan-out defensivo (`typeof X==='function'`) a ~10 funciones de render de otros módulos — riesgo medio; (3) `_initEventListeners()`, wiring legacy donde la mayoría de los comentarios dicen "migrado a X.js" pero todavía wirea nav/header/diálogo/tabs — hay que confirmar qué de esto sigue haciendo falta; (4) los 3 overrides de validación (`addGastoVar`/`addGastoFijo`/`addSpotify`), los de más cuidado por depender de que `gastos.js`/`spotify.js` ya hayan definido esas funciones antes. Hallazgo aparte: `openSheet` se monkey-patchea después en `deudores-personas.js` (código documentado como intencional) — cualquier extracción de esta zona tiene que preservar el orden de carga relativo con `gastos.js`, `spotify.js` y `deudores-personas.js`, no puede moverse a cargar antes en el `<head>`.
>
> **Bug propio, encontrado y corregido en el momento (extracción #1, `money-input.js`):** sacar una porción del *medio* de un bloque `<script>` sin cerrarlo antes rompe todo el bloque — HTML no tiene tags "anidados" dentro de un `<script>`, el primer `</script>` literal cierra el original sin importar qué haya en el medio. El método de validación que se venía usando en toda la sesión (buscar la primera línea con la subcadena `</script>`) tiene el mismo punto ciego ya conocido para comentarios, pero esta vez con consecuencia real: dio "OK" sobre un fragmento que no era lo que el navegador iba a ejecutar. Se corrigió cerrando/reabriendo el `<script>` alrededor del archivo nuevo, y se armó un validador consciente de comentarios HTML y del comportamiento real del tokenizer para esta y las próximas extracciones de esta zona. Detalle completo en `CHANGELOG.md#infraestructura--seguridad`.

> **Mapeo de `S`/`save()` (sesión posterior):** este bloque es bastante más que "el estado global" — son ~60 funciones de nivel superior, entre ellas casi todos los helpers que usa *literalmente cada módulo de la app*: `fmt`/`fmtNoCents`/`uid`/`hoy`/`escHtml`/`mesKey`/`parseMoney`/`parsePct`, el motor de mover plata (`descontarFuente`/`sumarFuente`/`getFuentes`/`getSaldoFuente`), `toast`/`dialogo`, `calcPatrimonioTotal`/`snapshotPatrimonio`, los helpers centralizados `_esGastoVarNoReal`/`_esEntradaEspejoNoIngreso` (ver `analisis-financiero.md` §9bis), y — hallazgo importante — **la definición base de `refresh()` vive acá adentro, al final del bloque**, no en el tercer bloque como se había etiquetado. `refresh()` llama directo (sin `typeof` guard) a `renderGastosVar`, `renderMesada`, `renderSpotify`, `renderDeudoresList`, `renderCustomCuentasList`, `renderTCDashboard`, etc. — funciones de ~7 módulos distintos. Esto es seguro *hoy* porque `refresh()` nunca se invoca sino hasta después de que todo terminó de cargar, pero significa que mover esta definición no es tan simple como "cortarla y pegarla en un archivo nuevo al final": si algún día algo dispara `refresh()` antes de tiempo, rompe fuerte.
>
> El bloque también tiene comentarios explícitos documentando *por qué* ciertos `<script src>` de módulos están geográficamente intercalados entre pedazos de este bloque — no es orden arbitrario: `spotify.js` carga justo al final de este bloque porque un wiring más abajo llama `addSpotify` de forma inmediata (no diferida), `mesada.js` carga después de `crearSplitWidget()` (de `js/core/split.js`) por la misma razón. **Esto es más flexible de lo que parece:** el requisito real es "cargar `S`/`save`/`refresh`/helpers antes que cualquier módulo los necesite" — si se externaliza todo este bloque a un archivo que cargue *primero* (antes que todos los `<script src>` de módulos), esa condición se sigue cumpliendo trivialmente, sin tener que preservar la posición geográfica exacta como sí hace falta en el bloque sheet-stack/nav (con `deudores-personas.js`).
>
> **Mapeo del tercer bloque (antes etiquetado "refresh() + menú Más"):** resultó ser una serie de IIFEs autoinvocadas independientes entre sí (`(function(){...})()` × 4+), una de las cuales es el segundo eslabón de la cadena de `window.refresh` (wrap para "gastos fijos", `_origRefreshFijos`) — el tercer eslabón ya vive en `js/core/mejoras.js` (con guard de `typeof window.refresh==='function'` + polling si todavía no existe, patrón correcto porque `function refresh(){}` de nivel superior sí cuelga de `window`, a diferencia del `const Events` que causó el bug anterior). Este bloque parece más liviano de dividir que los otros dos, al ser IIFEs ya auto-contenidas.

**Conclusión del mapeo, antes de extraer nada:** el bloque `S`/`save()` es el de mayor riesgo real de los tres — no por tamaño, sino porque contiene funciones que absolutamente todo el resto de la app da por sentado que existen como globales. La buena noticia es que su restricción de orden de carga es más simple de lo que parecía (cargar primero alcanza, no hace falta preservar posición geográfica). El tercer bloque (IIFEs) es candidato a dividirse primero, antes que `S`/`save()`, por ser más autocontenido.

> **Extracción del bloque de IIFEs, completa (sesión posterior):** dividido en 4 archivos — `mas-menu.js`, `sheet-viewport.js`, `gastos-fijos-progress.js`, `sheet-swipe.js` — sin sorpresas de monkey-patch (se verificó que ninguna función que este bloque expone en `window` se reasigna en otro lugar del archivo, a diferencia de `openSheet` con `deudores-personas.js`). Quedan **2 bloques núcleo** por resolver: `S`/`save()` (con `refresh()` adentro) y sheet-stack/nav. Detalle completo, incluida la lección aplicada sobre cerrar/reabrir `<script>` en cada corte, en `CHANGELOG.md#infraestructura--seguridad`.
>
> **Extracción de `S`/`save()`, completa (sesión posterior):** a diferencia de sheet-stack/nav, no hizo falta partirlo — nada antes de su posición original podía depender de su contenido (no existía todavía en ese punto del archivo), así que se extrajo entero a `js/core/core-state.js`, en la misma posición, como script clásico (no módulo, para que sus globales sigan siendo variables léxicas normales). Incluye la definición base de `refresh()` (primer eslabón de la cadena de 3 wraps). **Queda un solo bloque núcleo:** sheet-stack/nav — el que más cuidado necesita, por el monkey-patch de `openSheet` en `deudores-personas.js` que exige preservar el orden de carga relativo, a diferencia de `S`/`save()` que se pudo extraer sin esa restricción. Detalle en `CHANGELOG.md#infraestructura--seguridad`.

**Solución:** con los 3 restantes siendo exactamente el núcleo, sacar `'unsafe-inline'` de `script-src` es ahora, sin rodeos, una consecuencia directa de terminar el punto 4 (mover `S`/`save()`/`refresh()`/sheet-stack a `js/core/`). No queda ningún trabajo de CSP independiente de eso.

> **Cierre (sesión posterior):** se extrajo el último bloque núcleo — sheet-stack/nav (`openSheet`/`closeSheet`/`showScreen`/`applyModulos`, el wiring legacy de `_initEventListeners()` y los overrides de `addGastoVar`/`addGastoFijo`/`addSpotify`) — a un solo archivo nuevo, `js/core/sheet-stack.js`, unificando las dos mitades (A/B) en que vivía repartido alrededor de `money-input.js`. Se preservó el orden de carga relativo exigido por el monkey-patch de `openSheet` en `deudores-personas.js`: el `<script src>` nuevo quedó después de `gastos.js`/`spotify.js` (los overrides de `addGastoVar`/`addGastoFijo` leen esos globales a nivel superior del script, al parsear) y antes de `deudores-personas.js`. Con esto, `index.html` queda en **0 bloques `<script>` inline** — se sacó `'unsafe-inline'` de `script-src` en la CSP. `style-src` sigue con `'unsafe-inline'` por el CSS inline (44.7 KB), sin relación con este punto — ver advertencias pendientes. Detalle completo en `CHANGELOG.md#infraestructura--seguridad`.

> **Reapertura (sesión posterior, al probar en navegador real):** sacar `'unsafe-inline'` fue prematuro. `node --check` y las simulaciones con jsdom/`vm` (usadas para verificar el cierre de arriba) no aplican CSP, así que no podían detectar esto — recién apareció al probar en un navegador real, en consola: `Executing inline event handler violates ... script-src ...`. Dos causas, ninguna relacionada con `sheet-stack.js`: (1) el `onload="this.media='all'"` de los 2 `<link>` de carga async de CSS (Font Awesome, Google Fonts) — punto ciego real de la auditoría, nunca cubierto por el barrido de `onclick`/`onchange`/`oninput`/hover; arreglado con `js/core/async-css.js`, sin pendientes. (2) `onmouseenter`/`onmouseleave` generados dinámicamente vía `innerHTML` en varios módulos (botones "eliminar" con hover) — esto **sí** estaba documentado como pendiente en el propio código (`gastos.js`: *"pendiente de una clase :hover compartida"*), pero se pasó por alto que también cae bajo `script-src`. Se migraron los 2 pares de `gastos.js` a una clase CSS compartida (`.btn-delete-hover`); quedan sin revisar los "cinco módulos anteriores" que el comentario de `gastos.js` menciona (candidatos: `cuentas.js`, `prestado.js`, `encargos.js`, `tarjetas_credito.js`, `mesada.js`, sin confirmar). `'unsafe-inline'` se restauró en `script-src` hasta confirmar que ningún módulo genera algún atributo `on*` inline al renderizar. Detalle completo en `CHANGELOG.md#infraestructura--seguridad`.

> **Avance (sesión posterior):** se auditaron y corrigieron los 5 candidatos. Resultado: `cuentas.js` (6 casos — 2 pares hover/funcionales más delicados: `_renderMetaAportes()` y `renderMovsFiltros()` tenían `oninput`/`onchange` con lógica real, no solo estética, migrados a listeners delegados con guard, ver `CHANGELOG.md#infraestructura--seguridad`), `encargos.js` (2 casos), `tarjetas_credito.js` (2 pares hover) — corregidos. `mesada.js` y `prestado.js` salieron limpios. Se aprovechó para revisar también `spotify.js`, `deudores-personas.js`, `inicio.js`, `sheet-stack.js` y `async-css.js` (ya disponibles de sesiones anteriores) — los 5 limpios. **Aun así `'unsafe-inline'` se queda en `script-src`:** todavía faltan por revisar `js/core/movimientos.js`, `spotify-personas.js`, `encargos-personas.js`, `prestado-personas.js`, `configuracion.js`, `personas.js`, `analisis.js`, `actividad_reciente.js`, `alcancia.js`, `mas-menu.js`, `sheet-viewport.js`, `gastos-fijos-progress.js`, `sheet-swipe.js` — no disponibles en esta sesión.

> **Cierre definitivo (sesión posterior):** se auditaron los 13 módulos de pantalla restantes (`actividad_reciente.js`, `alcancia.js`, `analisis.js`, `movimientos.js`, `personas.js`, `spotify-personas.js`, `configuracion.js`, `encargos-personas.js`, `prestado-personas.js`, `mas-menu.js`, `sheet-viewport.js`, `gastos-fijos-progress.js`, `sheet-swipe.js`) — todos limpios — y después los 5 de infraestructura (`events.js`, `diferencial.js`, `split.js`, `core-state.js`, `money-input.js`). Único hallazgo real: `emptyState()` en `core-state.js` todavía aceptaba un `onclick="${btnFn}"` como string por compatibilidad hacia atrás — código muerto (los 3 callers reales, en `gastos.js`/`spotify.js`, ya usaban la forma nueva `{action, args}`), pero un riesgo real para el futuro. Se sacó la rama entera. Con los 29 archivos de la app confirmados, `'unsafe-inline'` se sacó de `script-src` de nuevo — detalle completo en `CHANGELOG.md#infraestructura--seguridad`.

### 2. Auditoría exhaustiva de `.innerHTML` (barrido parcial hecho, no exhaustivo)

**Estado:** se corrigió el bug de fondo de `escHtml()` (no escapaba comillas dobles — un no-op, afectaba a todos sus usos) y se hizo un barrido de las interpolaciones de campos de texto libre con nombres conocidos (`nombre`, `nota`, `notas`, `desc`, `descripcion`, `concepto`, `titulo`, `title`, `razon`) sin escapar dentro de `.innerHTML`/`toast()` — unos 110 casos corregidos. Detalle completo en CHANGELOG.

**Avance real:** al migrar Spotify aparecieron 3 casos más que ese barrido no había agarrado, porque el valor pasaba por una función (`spNombreDe(x)`) y no por el nombre de campo directo — dos en `.innerHTML` (nombre en la fila del integrante, nombre en un atributo `title`) y varios en `toast()`. Corregidos (ver `CHANGELOG.md#infraestructura--seguridad`). Al migrar Mesada se repitió exactamente el mismo patrón, con otra función distinta: `fuenteLabel()` devuelve el nombre de una cajita/cuenta personalizada (texto libre) sin escapar — 3 casos más corregidos en `abrirDetalleMesada()`. Al migrar Encargos se repitió una tercera vez, con el conteo más alto hasta ahora: ~18 casos, otra vez mayormente `fuenteLabel()` (desglose por cuenta, historial, previews de "yo puse la plata"/"ya la usé", traspaso, mover-cuentas, compra con TC) más `iniciales()` en un punto de `renderEncargosList` — corregidos en `CHANGELOG.md#encargos`. Al migrar Tarjetas de Crédito se repitió una cuarta vez, con 5 sitios: `tc.nombre` y `fuenteLabel(fuente)` interpolados directo en `toast()` (cupo insuficiente, saldo insuficiente), `fuenteLabel(p.fuente)` sin escapar en el badge de origen de un pago, una variable `descPago` (arma texto con `tc.nombre` + nota libre) insertada sin escapar, y el `<option>` del selector de cuenta de pago con `f.label` sin escapar — corregidos en `CHANGELOG.md#tarjetas-de-credito`. Al migrar Cuentas se repitió una quinta vez, con 12 sitios — el conteo más alto hasta ahora: nombre de cuenta personalizada y nombre de cajita sin escapar en `toast()` (crear/editar cuenta, cobrar CDT), nota/descripción libre del usuario sin escapar en cuatro `toast()` distintos (agregar dinero, sumar/restar en Nu, restar dinero), y `fuenteLabel()` sin escapar en el sheet de transferir (dos en `.innerHTML` del preview, tres en `toast()`) — corregidos en `CHANGELOG.md#cuentas`. Al migrar Inicio se repitió una sexta vez, con 1 solo sitio: `spNombreDe(p)` (nombre de persona en Spotify, texto libre) interpolado directo en el texto de un item de "Necesita atención" dentro de `renderAttencion()`, sin pasar por `escHtml()` — corregido en `CHANGELOG.md#inicio`. El resto de `renderAttencion()`, `renderHealthScore()` y `renderProyeccion()` (las otras funciones migradas con Inicio) se revisaron a mano contra el mismo patrón y no tenían más casos: solo interpolan números calculados, strings fijos del código, o valores que ya pasan por `escHtml()`/`fmt()` en otros puntos. Al confirmar Gastos (2026-07-23) se repitió una séptima vez, con 5 sitios: `fuenteLabel()` sin escapar en el badge de fuente de cada gasto, en dos `toast()` de error de `addGastoVar` y en el `toast()` de "compra cargada a" (con `tc.nombre`), más el `<option>` del selector propio de `abrirPagarGastoFijo()` sin escapar (mismo hallazgo puntual que TC — reimplementaba su selector a mano en vez de `poblarFuente()`). Al confirmar Plata Comprometida (2026-07-23) se repitió una octava vez, con 6 sitios: `item.cuentaDestino` sin escapar en la card principal, `tc.nombre` en dos ramas del plan de "Recibir", `_cpFuenteLabel(d.gastoCajita)` en las dos alertas de "Necesita atención", y `errores.join(', ')` en un `toast()`. **Al confirmar Alcancía (2026-07-23) la racha se corta:** no hay ningún sitio de `.innerHTML` sin escapar en código vivo — el único caso encontrado (`fmtFuente`, nombre de cajita/cuenta sin escapar) vive dentro de un bloque completo de "movimientos actuales" deshabilitado con `if(false)` (código muerto, no tocado — ver punto 3). El historial de alcancías destapadas, que sí es código vivo, solo interpola fechas y números. Confirma ocho veces seguidas (de nueve módulos posibles) el riesgo que ya describía este punto: el barrido por nombre de campo conocido no detecta texto libre que llega envuelto en una función — pero también confirma que no es automático: un módulo sin texto libre real que renderizar (como Alcancía en su código vivo) no lo repite. **Al migrar Configuración (2026-07-25) se repite el mismo resultado limpio de Alcancía:** los chips de categoría (`renderCatsConfig`) y los `toast()` de agregar/eliminar categoría ya interpolaban el nombre libre de la categoría envuelto en `escHtml()` en todos los sitios — segunda vez (de diez módulos) que este hallazgo no aparece. **Al migrar Actividad Reciente (2026-07-26) se repite un tercera vez:** las siete fuentes normalizadas (`_normMovimientos`, `_normGastos`, `_normDeudores`, `_normSpotify`, `_normEncargos`, `_normTC`, `_normCP`) arman `titulo`/`subtitulo` con texto libre sin escapar en el objeto intermedio, pero el único punto de render (`renderFeedActividad()`) pasa ambos campos por `esc()` antes de tocar el DOM — a diferencia de los módulos con el hallazgo, acá no hay múltiples sitios de salida que puedan quedar sin cubrir, solo uno. **Al confirmar Análisis contra su código fuente (2026-07-28) se repite una novena vez, con un campo nuevo:** `cat` (nombre de categoría) sin escapar en 5 sitios — Top categorías del mes, label + atributo `data-cat` de Presupuestos, barra de progreso de Presupuestos, y el `toast()` de aviso al 80%. A diferencia de todos los anteriores (que reincidían sobre `fuenteLabel()`/`nombre`/`nota`), acá el campo sin escapar nunca había aparecido en este hallazgo — confirma que no basta con revisar los nombres de campo ya conocidos, cualquier texto libre nuevo que se agregue al modelo de datos puede repetir el patrón. Corregido esta sesión. **Al confirmar Personas contra su código fuente (2026-07-28) sale limpio** — tercera vez (de trece módulos) que no aparece, junto con Alcancía y Configuración: los ~15 sitios donde interpola texto libre (`nombre`, `nota`, iniciales, término de búsqueda) ya pasan por `escHtml()`. **Al confirmar Préstamos contra su código fuente (2026-07-30) se repite una décima vez, con un giro distinto a los anteriores:** el módulo ya era cuidadoso en general (49 usos de `escHtml()`, incluyendo la mayoría de sus `toast()` con nombre de persona) — pero **los dos `toast()` de "recién creado" se quedaron sin envolver** (`addDeudor()` y `crearMiDeuda()`), mientras que los de eliminar, error y advertencia de saldo sí escapaban bien el mismo campo. A diferencia de los nueve hallazgos anteriores (donde solía faltar en todos los sitios de un módulo, o en ninguno), acá el patrón se rompió justo en dos de más de una decena de sitios similares dentro del mismo archivo — confirma que "el módulo ya es cuidadoso" tampoco es garantía completa, hay que revisar cada sitio de salida individualmente. Corregido esta sesión.

**Por qué sigue siendo un hallazgo:** el barrido se hizo por nombre de campo conocido, no revisando uno por uno los usos de `.innerHTML` del resto de la app. Cada módulo que se migre de acá en más debería revisarse a mano contra este mismo patrón (texto libre envuelto en una función auxiliar) antes de darlo por cerrado, como ya se hizo con Spotify.

**Hallazgo puntual nuevo, todavía sin resolver:** `buildFuentesOptsHtml()` — función núcleo compartida por TODA la app para poblar selectores de cuenta (`poblarFuente()` la usa desde gasto variable, gasto fijo, y ahora también desde el flujo de pago de TC) — tiene el mismo problema: interpola `f.label` (nombre de cajita/cuenta personalizada, texto libre) sin escapar. Se detectó al revisar Tarjetas de Crédito, que reimplementaba manualmente su propio selector en vez de usar esta función compartida (por eso el fix de TC se aplicó en el sitio de la reimplementación, no acá). No se tocó esta función por el mismo criterio que ya se aplicó a `toast()`: es núcleo compartido, y cambiarla de paso podía afectar pantallas fuera de este alcance. Como usa `f.label` en el texto de un `<option>` (no un atributo), el riesgo real de inyección es menor que en `.innerHTML`/`toast()` — pero sigue siendo el mismo patrón de fondo y conviene cerrarlo en una pasada dedicada al núcleo compartido.

**Hallazgo puntual nuevo, todavía sin resolver (2026-07-26, al extraer `js/core/busqueda-global.js`):** el mensaje de "Sin resultados para..." interpola directo el término buscado (`q`, texto libre escrito por el usuario en el input de búsqueda) sin pasar por `escHtml()`. Es preexistente a esta sesión — la extracción a `js/core/` movió el código tal cual, sin tocar su contenido — pero vale la pena registrarlo como el undécimo caso de este patrón (de catorce módulos revisados), esta vez en un texto que el propio usuario escribe en el momento, no un campo guardado en `S`. No se corrigió de paso por ser una extracción quirúrgica (solo mover código, no tocar lógica); queda para una sesión dedicada a este módulo.

**Solución de fondo (pendiente):** la única forma de cerrar esto de raíz y para siempre es dejar de construir HTML con template strings + `.innerHTML` y migrar a un método que escape por defecto (ej. `textContent` para nodos de solo texto, o una función `h()`/`render()` que fuerce escapado salvo que se pida explícitamente lo contrario). Cambio grande, no se intentó. **Hallazgo puntual nuevo, todavía sin resolver:** `toast()` sigue renderizando su mensaje con `innerHTML` sin escapar — no se tocó porque al menos 3 llamadas de otros módulos le pasan HTML intencional (íconos), y cambiarla de paso hubiera afectado pantallas fuera del alcance de esa sesión. Cuando se toque el núcleo compartido, separar el ícono (HTML de confianza) del mensaje (texto, debe ir escapado).

### 3. Arquitectura monolítica

**Avance real:** cinco módulos extraídos. Spotify pasó de vivir inline en `index.html` a `js/modules/spotify.js` + `js/modules/spotify-personas.js` (dos archivos, no uno — por una razón real de orden de carga, no estética; ver `CHANGELOG.md#infraestructura--seguridad`). Mesada pasó a `js/modules/mesada.js` — acá un solo archivo alcanzó, porque su única dependencia real (`crearSplitWidget`, motor compartido con Encargos y "Yo debo") ya estaba definida en un único punto del documento. Encargos pasó a `js/modules/encargos.js` + `js/modules/encargos-personas.js` — mismo motivo de partir en dos que Spotify (la integración con Personas depende de funciones definidas más abajo en el documento); el motor de diferencial/margen y el de split se quedaron en `index.html` porque los comparte con Préstamos, solo se movieron las instancias que Encargos registra en ellos. **Tarjetas de Crédito** pasó a `js/modules/tarjetas_credito.js` — un solo archivo, sin dependencia real de orden de carga *en el momento de esa migración* (no usa `crearSplitWidget` ni nada definido más abajo) — **dejó de ser cierto el 2026-07-26**, cuando `abrirDetalleMov()`/`eliminarMovimiento()` se extrajeron de `index.html` a `js/core/movimientos.js` (que carga después de TC) sin revisar que `tarjetas_credito.js` ya las referenciaba directo: `ReferenceError` real en consola al cargar la app, corregido el 2026-08-03 con el mismo wrapper que `prestado.js` ya usaba para el mismo caso — ver `CHANGELOG.md#tarjetas-de-crédito` — pero con una complicación distinta a las de Spotify/Encargos: el `<script>` original no se podía extraer como bloque contiguo, porque compartía el tag con `navTo()` (navegación global de las 13 pantallas, no solo TC) y con el "Feed de actividad financiera" (módulo sin relación real con TC que vivía ahí por casualidad de cómo se fue armando el archivo con el tiempo). Se extrajo función por función en vez de por límites de `<script>`, dejando `navTo()` y el Feed en `index.html`. **Cuentas** pasó a `js/modules/cuentas.js` — el módulo más grande hasta ahora (Nequi, Efectivo, cuentas personalizadas y todo el subsistema de Nu), también un solo archivo sin dependencia real de orden de carga; repitió la misma complicación de TC en menor escala (el preview del sheet "crear CDT" compartía `<script>` con Tarjetas de Crédito sin relación real — se extrajo aparte). **Inicio** pasó a `js/modules/inicio.js` — a diferencia de los cinco anteriores, no es un módulo con sheets ni formularios: es la pantalla de resumen/dashboard. Su código estaba disperso en varios `<script>` distintos del documento (no uno solo), cada uno compartido con lógica no relacionada — mismo patrón ya visto en TC y Cuentas, pero repetido varias veces dentro del mismo módulo: `renderAttencion()` vivía junto al resto del núcleo de refresco; `calcHealthScore()`/`renderHealthScore()` y `renderProyeccion()` compartían un IIFE grande con "Ocultar/mostrar saldos" y "Búsqueda global" (ninguno de Inicio); `_checkGastoAlto()` compartía bloque con `markDirty()`/`saveAndRefresh()` (núcleo, no de Inicio); y el reposicionamiento de "Necesita atención" compartía IIFE con el progreso de gastos fijos (tampoco de Inicio). Se extrajo función por función, no por límites de `<script>`, dejando en `index.html` todo lo que era realmente compartido — incluyendo `refresh()` mismo (el orquestador central) y el hook `_renderMejoras()` (compartido con Análisis/Presupuestos). Ver nota de 2026-07-22 arriba para el detalle completo. **Actividad Reciente** pasó a `js/modules/actividad_reciente.js` — repite exactamente el hallazgo de TC del que había quedado colgando desde el 2026-07-20: el "Feed de actividad financiera" compartía `<script>` con `navTo()` y con Tarjetas de Crédito por casualidad de cómo se fue armando el archivo, sin relación real con ninguno de los dos. Se cerró esta vez: el IIFE completo del feed salió de ese `<script>`, `navTo()` se quedó donde estaba (sigue siendo núcleo compartido por las 13 pantallas), y el nuevo archivo se carga con `<script src>` en el mismo punto exacto del documento — necesario porque el módulo envuelve `window.refresh()` al cargar, sin esperar a ningún evento, y esa envoltura exige que `refresh()` ya exista en ese punto. No todos los módulos van a necesitar partirse en dos como Spotify y Encargos, ni todos van a poder extraerse por límites de `<script>` como Mesada — depende de qué código no relacionado haya terminado compartiendo el mismo bloque a lo largo del tiempo. `index.html` bajó de 17.916 a 16.962 líneas tras TC, a 11.381 tras Cuentas, a 10.934 tras Inicio, y a 10.542 tras Actividad Reciente (verificado sobre el archivo recibido al empezar cada sesión). **Nota de consistencia:** el archivo recibido para esta sesión tenía 13.864 líneas, no las 16.962 que este documento registraba como cifra posterior a TC — mismo tipo de discrepancia ya anotada sin investigar tras Encargos; no se investigó tampoco esta vez por no ser el alcance de la sesión, pero ya son dos veces seguidas — vale la pena revisar de dónde sale la diferencia antes de seguir confiando en estas cifras de línea como medida de avance. Se mantuvo como `<script src>` clásicos (no ES modules) en los cinco casos, a propósito: el estado global (`S`) y los helpers (`save`, `escHtml`, `toast`, etc.) siguen sin extraer, y forzar `import/export` solo para un módulo hubiera exigido tocar ese núcleo compartido de paso — el cambio de mayor riesgo de este punto, que sigue pendiente como se describe abajo.

**Nota adicional (2026-07-23):** se confirmó que **Gastos, Plata Comprometida y Alcancía** también ya estaban extraídos a `js/modules/gastos.js`, `js/modules/plata_comprometida.js` y `js/modules/alcancia.js` — este documento no lo tenía registrado (ver nota de esa fecha arriba, mismo caso que Préstamos el 2026-07-20). Los tres son archivo único, sin necesidad de partirse en dos: Gastos y Alcancía no dependen de nada definido más abajo en `index.html` (mismo caso que Mesada); Plata Comprometida tampoco depende de Personas — usa `S.deudores` directo (mismo caso que TC). Alcancía tiene una particularidad de arquitectura que no se había visto en los ocho módulos anteriores: su pantalla (`screen-alcancia`) sigue siendo HTML estático dentro de `index.html` (ya con sus `data-action` wireados), mientras que sus tres sheets se inyectan dinámicamente desde el propio módulo — un híbrido entre el patrón "todo estático" (Spotify, Mesada, etc.) y "todo inyectado" (Plata Comprometida, que arma hasta su propia pantalla por JS). Con estos tres, ya son **nueve módulos** confirmados fuera de `index.html`, de los diez que tienen `.js` propio (falta solo la nota de detalle de Préstamos, ya migrado pero sin documentar aquí).

**Nota adicional (2026-07-25):** se migró **Configuración** a `js/modules/configuracion.js` — décimo módulo confirmado. Archivo único, sin dependencia real de orden de carga (mismo caso que Mesada/Gastos/Alcancía). A diferencia de todos los anteriores, no vivía en un único `<script>` compartido con código no relacionado, sino repartido en **tres bloques distintos**: categorías personalizadas + backup JSON (dentro del bloque núcleo grande, junto a `S`/`save`/`escHtml`), `toggleModulo`/`borrarTodo` (otro bloque más abajo) y `exportarCSV` (compartiendo un IIFE numerado por secciones con Búsqueda global y Presupuestos — la misma IIFE de la que ya se habían sacado las secciones de Inicio). Se extrajo función por función, no por límites de `<script>`, mismo método que TC/Cuentas/Inicio. Quedó fuera del módulo, a propósito: `applyModulos()` (toca Cuentas/Mesada/Inicio, no solo esta pantalla), el auth de Firebase completo, y el gate de PIN/biometría (arranca con toda la app, no solo desde Configuración). `index.html` bajó de 10.934 a 10.758 líneas.

**Nota adicional (2026-07-27):** se confirmaron dos migraciones más sin registrar — `js/modules/analisis.js` (Ingresos Fijos + Presupuestos, antes repartido en tres bloques sueltos de `index.html`, uno de ellos el mismo IIFE numerado por secciones ya mencionado al migrar Inicio y Configuración) y `js/modules/personas.js` (con su propia `personas.md`, no leída esta sesión). Ninguno de los dos se recibió como archivo aparte, solo se pudo confirmar su existencia y wiring desde `index.html` (`<script src>`, comentarios de migración, y los `data-action` estáticos que quedan visibles) — no hay verificación línea por línea de su contenido, a diferencia del resto de este documento. `index.html` bajó a **9.249 líneas** en el archivo recibido esta sesión, contra las 10.542 registradas tras Actividad Reciente (2026-07-26) — una caída de 1.293 que coincidiría con sacar Análisis y Personas, pero no se puede atribuir con precisión a uno u otro sin ver `antes.html` de este tramo, mismo tipo de salto sin reconciliar ya visto varias veces en este documento (tras Encargos, tras TC, y con el conteo de Actividad Reciente el 2026-07-26).

**Nota adicional (2026-08-01):** se extrajo `js/modules/deudores-personas.js` — el selector de persona compartido entre "Agregar persona" (Me deben) y "Nueva deuda" (Yo debo), que hasta esta sesión seguía inline en `index.html` pese a que el resto de Préstamos (`prestado.js`, `prestado-personas.js`) ya estaba migrado (ver nota de esta fecha arriba). Con esto, Préstamos pasa a ser el segundo módulo (después de Spotify/Encargos, que se dividieron en dos) en necesitar más de un archivo de integración con Personas — en su caso, tres en total. `index.html` bajó de 9.252 a 9.123 líneas.

**Nota adicional (2026-07-26):** se extrajo **`js/core/movimientos.js`** — `abrirDetalleMov()` y `eliminarMovimiento()`, las dos únicas funciones que quedaban sueltas en `index.html` pese a que ya son 13 los módulos migrados (ver nota del 2026-07-22, cuando se registraron bajo el namespace `core:` en vez de `cuentas:` por ser núcleo compartido por toda la app, no de un dominio en particular). A diferencia de todos los módulos de este punto, este archivo no es un módulo de dominio — va en `js/core/`, junto a `events.js`, no en `js/modules/`. Sin dependencia real de orden de carga (ninguna de las dos se llama en el momento en que el script se parsea, solo vía `data-action` en tiempo de click), así que no hizo falta partir el archivo en dos ni reordenar ningún `<script src>` existente: se cargó en el mismo punto donde antes vivía el registro `Events.on` suelto, que ahora se movió adentro del propio archivo (mismo patrón que usa cada módulo para registrarse a sí mismo). De paso se corrigieron dos bugs reales encontrados en `eliminarMovimiento()` — detalle en `CHANGELOG.md`, sección "Núcleo compartido — detalle y eliminación de movimientos", no repetido acá por ser hallazgos ya resueltos, siguiendo la convención de este documento. `index.html` bajó de 9.123 a 8.617 líneas.

**Nota adicional (2026-07-26, misma fecha, sesión distinta):** se extrajo **`js/core/busqueda-global.js`** — el bloque "2. BÚSQUEDA GLOBAL" (304 líneas) del IIFE compartido que también contenía "Ocultar/mostrar saldos" y, ya solo como comentarios, las secciones de salud financiera/proyección/presupuestos/CSV migradas en sesiones anteriores (ver notas de Inicio y Configuración arriba). Va en `js/core/`, no en `js/modules/`, mismo criterio que `movimientos.js`: busca sobre `S.gastosVar`, `S.gastosFijos`, `S.deudores`, `S.cajitas`, `S.cuentasPersonalizadas`, `S.encargos`, `S.personas`, `S.misDeudas`, `S.spotifyPersonas`/`S.spotifyHistorial` y `S.movimientos` — es transversal por naturaleza, no de un dominio en particular. **Sin dependencia real de orden de carga:** cada navegación a un módulo específico (`abrirDeudor`, `abrirEncargoDetalle`, `abrirCuenta`, `abrirMiDeuda`, `abrirDetalleCajita`, `abrirPerfilPersona`, `spNombreDe`, `spPersonaPagadaVigente`) ya se resolvía en tiempo de click vía `typeof X === 'function'`, no al parsear el script — mismo patrón que ya permitió extraer `movimientos.js` sin partir el archivo ni tocar el orden de otros `<script src>`. Se cargó en el mismo punto exacto donde antes vivía el IIFE (justo después de cerrar `</script>` del bloque que se quedó con "Ocultar/mostrar saldos" y el resto de comentarios de migración). `index.html` bajó de 8.617 a 8.320 líneas (-297, descontando las 6 líneas de comentario que quedaron en su lugar). Sin hallazgos nuevos de `.innerHTML` sin escapar: el único texto libre que interpola (`q`, el término buscado, en el mensaje de "Sin resultados") no pasa por `escHtml()` ni antes ni después de esta extracción — es un hallazgo preexistente, no introducido por el movimiento de código, y se anota como pendiente en el punto 2 de este documento en vez de corregirse de paso (fuera del alcance de una extracción quirúrgica).

Queda pendiente, y es harina de otro costal: **`_initEventListeners()` (~262 líneas, `index.html`)** — el delegado de eventos original de antes de que existiera `Events`/`data-action`, que sigue cableando con `addEventListener` directo botones de ~15 dominios ya migrados (Gastos, Spotify, Mesada, Cuentas, CDT, metas de cajita, previews de Encargos/TC, Transferir, Nequi/Efectivo, etc.). A diferencia de Búsqueda global, esto no es un módulo autocontenido sino un cajón de sastre repartido entre módulos ya existentes — repartirlo bien exige tocarlo módulo por módulo (mover cada listener a su archivo dueño), no en una sola pasada. No se tocó esta sesión.

**Nota adicional (2026-07-26, misma fecha, tercera sesión):** primera redistribución real de `_initEventListeners()` (ver nota anterior) — se movieron los 12 listeners de **Mesada** (`btn-anio-prev/next`, `btn-confirmar-mesada`, `btn-confirmar-mesada-pend`, `mpDestino/mpMonto`, `mpDebeWrap/mpQuedaDebiendo`, `mppDestino/mppMonto`, `mpSplitToggle`, `btn-add-split-row`) a `js/modules/mesada.js`, y los 8 de **Spotify** (`btn-add-spotify-persona`, `btn-pagar-spotify`, `btn-guardar-spotify`, `btn-confirmar-sp-destino`, `spMesesSelect`, `btn-confirmar-pagar-spotify`, `spPagarFuente/spPagarMonto`) a `js/modules/spotify.js`. En total, **20 de los ~80 listeners quedaron redistribuidos**; los ~15 dominios restantes (Gastos, Cuentas, CDT, metas de cajita, previews de Encargos/TC/Préstamos, Transferir) siguen pendientes, uno por sesión.

- **No es una migración de `onclick` → CSP** (ya eran `addEventListener` directo, sin problema de CSP) — es pura reorganización de código: mover cada listener a su módulo dueño en vez de dejarlo mezclado con el de otros ~15 dominios en `index.html`.
- **Sin dependencia real de orden de carga:** se verificó línea por línea que todos los ids del DOM que estos listeners enganchan ya existen en el HTML estático antes de donde cargan `mesada.js`/`spotify.js` (línea 5718-5719), así que el wiring se agregó como código de nivel superior (se ejecuta al parsear el módulo), sin envolver en `DOMContentLoaded`.
- **Dos casos que se quedaron en `index.html` a propósito, no por descuido:** `btn-guardar-ingreso-fijo` (ya documentado como de `js/modules/analisis.js`) y el resto de los ~60 listeners de otros dominios — no se tocaron esta sesión.
- **Verificado con jsdom**, no solo `node --check`: se simuló el DOM real con los ids involucrados y se disparó cada evento (`click`/`change`/`input`) contra los archivos ya editados, confirmando que `cambiarAnio` sigue moviendo `S.mesadaAnio`, que `mpDebeWrap` sigue delegando el click al checkbox `mpQuedaDebiendo` (incluyendo el caso real donde `_syncMpDebeWrap()` lo vuelve a ocultar si el monto no amerita marcar pendiente), que `mpSplitToggle`/`btn-add-split-row` siguen llamando al motor compartido `splitToggle('mp')`/`splitAgregarRow('mp')`, y que los botones de Spotify siguen abriendo los sheets correctos y disparando `addSpotify`/`confirmarPagarSpotify` (validado por los mismos mensajes de `toast()` de error que antes). 17 casos verificados, 0 fallos.
- `_initEventListeners()` en `index.html`: 262 → ~215 líneas (quedan comentarios de referencia en el lugar de cada bloque movido, siguiendo la misma convención ya usada para Configuración/Análisis).

Queda pendiente el resto — sugerido para las próximas sesiones, un dominio a la vez: **Gastos** (5 listeners, ya autocontenido), luego **Cuentas** (el grupo más grande — cuenta custom, CDT, meta de ahorro, Nequi/Efectivo, Nu, transferir), y por último **Encargos/Tarjetas de Crédito/Préstamos** (los previews del motor Diferencial, que están entrelazados entre los tres módulos y conviene tocarlos juntos). Necesito el `.js` de cada uno antes de tocarlo, mismo criterio que esta sesión.

**Nota adicional (2026-07-26, misma fecha, cuarta sesión):** segunda redistribución de `_initEventListeners()` — **Gastos** (`js/modules/gastos.js`), 7 listeners: `.btn-open-gasto-var/fijo`, `btn-guardar-gasto-var/fijo`, `btn-confirmar-pagar-gf`, `pgf-fuente`, `gv_fuente`. Con esto van **27 de ~80 listeners redistribuidos**; quedan Cuentas (el grupo más grande) y Encargos/TC/Préstamos (entrelazados, a tocar juntos).

- **Discrepancia encontrada y resuelta:** el propio encabezado de `gastos.js` documentaba la convención *anterior* (controles estáticos de sheets se quedan en el wiring centralizado de `index.html`), que es la contraria a la que se estableció para Mesada/Spotify en la sesión pasada. Se actualizó el comentario para que ya no quede documentación contradictoria — la convención vigente desde el 2026-07-26 es que cada módulo hace el wiring de sus propios controles, estáticos o no.
- **Dedupe de paso:** las cards del menú "+" (`menu-gasto-var`/`menu-gasto-fijo`, que se quedan en `index.html` por ser parte del FAB compartido con Cuentas) reimplementaban a mano `poblarCatSelect+openSheet` en vez de llamar a `abrirNuevoGastoVar()`/`abrirNuevoGastoFijo()`, que ya existían en `gastos.js` para el botón del estado vacío. Se cambiaron para delegar a esas funciones — mismo comportamiento, sin la lógica duplicada.
- **Bug real encontrado y corregido, con eco hacia la sesión anterior:** `addGastoVar`, `addGastoFijo` (Gastos) y `addSpotify` (Spotify) se **sobrescriben más abajo** en `index.html`, dentro de `_injectErrorSpans()` (línea ~6878), para agregarles validación inline con mensajes de error junto a cada campo. Esa sobrescritura corre *antes* de que `_initEventListeners()` se invocara (línea ~6928) en el código original, así que el botón terminaba enganchado a la versión ya validada. Al mover el wiring a los archivos de módulo — que cargan y ejecutan al parsear, mucho antes de esa sobrescritura — la primera versión de este cambio capturó la función original sin validar, para las tres. Mismo patrón que ya está documentado como aprendizaje del proyecto (`editarSpotify` + Personas): la referencia se resuelve al momento de `addEventListener`, no en cada click. **Se corrigió en el mismo archivo de esta sesión (`gastos.js`) y retroactivamente en `spotify.js`** (entregado la sesión anterior, antes de detectar el problema): en vez de pasar la función directa, se pasa una flecha `() => addGastoVar()` que resuelve el global en vivo en cada click. Mesada no tiene este problema — ninguna de sus funciones se sobrescribe en otro lado.
- **Verificado con un test de jsdom dedicado** que reproduce el orden real (cargar el módulo primero, simular la sobrescritura de validación después, disparar el click) para los tres casos — los tres terminan invocando la versión sobrescrita, no la original. Además, los 5 listeners restantes de Gastos se probaron igual que en la sesión anterior (botones fantasma delegando a `abrirNuevoGastoVar/Fijo`, `gv_fuente`→`mostrarAlertaFuente('gv')`, `pgf-fuente`→`pgfActualizarSaldo`, `btn-confirmar-pagar-gf`→`confirmarPagarGastoFijo`).
- `_initEventListeners()`: ~215 → ~200 líneas.

Queda pendiente: **Cuentas** (el grupo más grande — cuenta custom, CDT, meta de ahorro, Nequi/Efectivo, Nu, transferir) y **Encargos/Tarjetas de Crédito/Préstamos** (los previews del motor Diferencial, entrelazados entre los tres). Antes de tocar cualquiera de los dos, conviene repetir la misma revisión que reveló el bug de esta sesión — buscar overrides tardíos de las funciones involucradas en cada uno.

**Nota adicional (2026-07-26, misma fecha, quinta sesión):** tercera redistribución de `_initEventListeners()` — **Cuentas** (`js/modules/cuentas.js`), el grupo más grande hasta ahora: **31 listeners** movidos (cuenta custom, CDT, meta de ahorro en cajita, agregar/restar dinero, editar apertura, transferir, selector de cuentas, Nequi/Efectivo, Nu entró/salió plata, agregar cajita, menú combinado de agregar dinero, tasa EA de Nu). Con esto van **58 de ~80 listeners redistribuidos**; queda por redistribuir Encargos/TC/Préstamos (entrelazados vía el motor Diferencial, a tocar juntos en la próxima sesión), más un puñado de casos que se quedan en `index.html` a propósito, no pendientes de redistribuir (ver detalle abajo): el color picker de avatares de **Personas** y las cards del FAB "+" de Gastos/Cuentas.

- Todas las funciones destino (`abrirNuevaCuenta`, `confirmarCrearCDT`, `guardarMetaCajita`, `confirmarAgregarDinero`, `abrirCuenta`, `confirmarNuMovimiento`, `addCajita`, `actualizarAdMenuSaldo`, `actualizarTransfPreview`, etc.) ya vivían en `cuentas.js` desde su migración del 2026-07-22 — esta sesión solo movió el wiring (`addEventListener`), no las funciones.
- **Dos casos que a primera vista parecían de Cuentas y no lo eran — se dejaron en `index.html` a propósito:**
  - `mov_fuente` (sheet "Registrar movimiento") es de **Préstamos**, no de Cuentas, pese a que el nombre del id se parece al de otros selectores de fuente ya migrados. Queda para la sesión de Encargos/TC/Préstamos.
  - El selector de color de avatar (`[data-pick-color]`) es del sheet "Nueva persona" (**Personas**), no de Cuentas. Se encontró además que `selColor()`, la función que este listener invoca, **no está definida en ningún archivo revisado** — posible referencia rota preexistente, no introducida por esta sesión. No se investigó ni se corrigió por no ser el alcance (dominio Cuentas); queda anotado para cuando se audite Personas.
  - Las cards del FAB "+" (`menu-agregar-dinero`, `menu-transferir`) abren flujos de Cuentas pero se quedaron en `index.html`: son parte del mismo menú compartido con Gastos (`menu-gasto-var`/`menu-gasto-fijo`), mismo criterio ya documentado el 2026-07-26 en la segunda sesión.
- **Verificado:** sintaxis de `index.html` (los 19 bloques `<script>` inline concatenados) y de `cuentas.js` con `node --check`, sin errores; se revisó línea por línea que ninguno de los 31 nombres de variable movidos (`btnCDTConf`, `adMenuDest`, etc.) colisionara con una declaración `const`/`let` ya existente en otro punto del documento — todos vivían antes exclusivamente dentro del scope de función de `_initEventListeners()`, así que no había riesgo real, pero se confirmó igual antes de mover nada a nivel superior de `cuentas.js`.
- `_initEventListeners()` en `index.html`: ~200 → **141 líneas**.

Queda pendiente: **Encargos/Tarjetas de Crédito/Préstamos** — los previews del motor Diferencial (`movenc_dif_real`, `ctc_dif_real`, `usar_parte_dif_real`, `prtc_dif_real`) y los demás controles con `oninput`/`onchange` de esos tres sheets, entrelazados entre sí. Con esto se termina la redistribución activa, pero **no se cierra `_initEventListeners()` del todo**: seguirán viviendo ahí, a propósito y sin plan de moverse, el wiring núcleo genérico compartido por las 13 pantallas (nav, dialog, close-sheet delegado, `data-save-refresh`), las cards del FAB "+" de Gastos/Cuentas (comparten menú), el tab-bar de Gastos (wiring genérico pero enganchado a `switchGastoTab()`, de `gastos.js`) y el color picker de avatares de Personas (`[data-pick-color]` → `selColor()`, con el bug de función no definida ya anotado arriba). Antes de tocar Encargos/TC/Préstamos, repetir la revisión de overrides tardíos (mismo chequeo que reveló el bug de `addGastoVar`/`addSpotify` en la sesión anterior).

**Nota adicional (2026-07-27):** se cerró la redistribución con `encargos.js`, `prestado.js` y `tarjetas_credito.js` disponibles. **Corrección al párrafo anterior:** el grupo nunca fue "Encargos/TC/Préstamos" — se revisó `tarjetas_credito.js` línea por línea y no referencia ni un solo `ctc_*`/`movenc_*`/`mov_*`. Los campos `ctc_*` (`ctc_monto`, `ctc_cuenta_enc`, `ctc_tarjeta`, `ctc_destino`, `ctc_dif_real`) son del sheet "Compra con TC", que es un flujo de **Encargos** (gastar plata de un encargo cargándola a una tarjeta de crédito), no de Tarjetas de Crédito — el nombre "ctc" venía confundiendo esta distinción desde que se registró la primera vez. El grupo real, y el único que se redistribuyó esta sesión, fue **Encargos + Préstamos**; Tarjetas de Crédito no tenía nada pendiente acá.

- **Movido a `encargos.js`:** `movenc_monto`→`_movEncSplitPreview`, `movenc_mia_cuenta_sale`/`_entra`→`_movEncMiaPreview`, `ctc_monto`/`ctc_cuenta_enc`/`ctc_tarjeta`/`ctc_destino`→`_ctcActualizarPreview`, y los previews de Diferencial `movenc_dif_real`→`_difResumen`, `ctc_dif_real`→`_ctcDifResumen`, `usar_parte_dif_real`→`_usarParteDifResumen`. Insertado como bloque nuevo antes de `Events.registerAll('encargos', ...)`.
- **Movido a `prestado.js`:** `mov_desde_encargo`, `mov_enc_sel`, `mov_enc_cuenta`, `mov_tiene_extra`, `mov_extra_monto`, `prtc_dif_real`→`_prtcDifResumen`, `mov_fuente`→`mostrarAlertaFuente('mov')`. `_onMovMontoInput` se fusionó dentro del listener de `mov_monto` que el archivo ya tenía (`_updatePrestSplitResumen`) para no dejar dos `addEventListener` separados sobre el mismo campo.
- **Sin dependencia de orden de carga:** las funciones movidas ya eran declaraciones `function` (hoisted) dentro del mismo archivo destino donde vivían desde antes — mismo criterio de verificación ya usado con Cuentas, sin necesidad de tocar ningún `<script src>`.
- **Verificado:** `node --check` sin errores en los tres módulos y en los bloques `<script>` inline de `index.html` concatenados.
- `_initEventListeners()` en `index.html`: 141 → **~118 líneas**. Con esto se cierra la redistribución activa del todo. Lo que queda ahí a propósito, sin plan de moverse (detallado en la nota del 2026-07-26 de arriba): wiring núcleo genérico, FAB de Gastos/Cuentas, tab-bar de Gastos. El color picker de Personas ya no vive acá — ver nota siguiente.

**Nota adicional (2026-07-27, mismo día, corrección posterior):** la nota anterior de este mismo día estaba mal. `selColor()` **sí existe** — se encontró en `prestado.js` (junto a `npColorSel`, `initColorPicker()` y el manejo de `.avatar-color-opt`), y `addDeudor()` la usa para guardar el color del nuevo deudor. No se había revisado `prestado.js` en el momento de escribir esa nota (recién se subió después) — la afirmación "no está definida en ningún archivo" fue apurada. El sheet "Nueva persona" es en realidad el alta de **Deudores** (Préstamos), no del sistema genérico de Personas — el nombre del sheet confunde. Se restauró el listener, movido a `prestado.js` en vez de dejarlo borrado. En ese momento se anotó `btn-crear-deudor` sin wiring como "hueco real" pendiente de `deudores-personas.js` — la nota siguiente, con ese archivo en mano, cierra el hallazgo con un resultado distinto.

**Nota adicional (2026-07-27, mismo día, tercera sesión):** con `deudores-personas.js` disponible, el hallazgo real es más grande de lo que se pensaba: no es que a `btn-crear-deudor` le faltara wiring puntual — **el sheet `#sheet-nueva-persona` completo nunca se muestra**. `deudores-personas.js` sobrescribe `openSheet()` e intercepta `id==='nueva-persona'` con un `return` antes de invocar el original, redirigiendo a `abrirSelPersona(_onSelPersonaMeDeben)` (el selector genérico de Personas). En consecuencia, son código muerto en la práctica: el sheet en sí, su color picker, `selColor()`, `npColorSel`, `initColorPicker()` y `addDeudor()` — con o sin el listener restaurado en la nota anterior, ninguno de ellos tiene ya una vía de ejecución real. No se borró nada — se documentó en `prestado.js` con un comentario nuevo, mismo criterio que el código muerto ya anotado antes en el proyecto (`toggleCDT()`/`toggleCajita()` en Cuentas, el bloque `if(false)` de Alcancía). Se descarta la lectura anterior de "hueco real que falta wiring": no falta un wiring, sobra una ruta entera que quedó reemplazada por la integración con Personas sin retirar el código viejo.

Para el resto: sin cambios desde la revisión anterior. ~1.3 MB repartidos ahora en `index.html` + 13 archivos de módulo confirmados (Spotify, Mesada, Encargos, TC, Cuentas, Inicio, Actividad Reciente, Gastos, Plata Comprometida, Alcancía, Configuración, Análisis, Personas) + 2 archivos núcleo compartido (`js/core/movimientos.js`, confirmado 2026-07-26; `js/core/busqueda-global.js`, extraído 2026-07-26) más cuatro archivos de integración con Personas aparte (`spotify-personas.js`, `encargos-personas.js`, `prestado-personas.js`, `deudores-personas.js` — este último confirmado 2026-08-01; los otros tres, cargados en `index.html` pero no revisados en detalle), cientos de funciones globales, 13 pantallas y ~46+ sheets sin separación real de responsabilidades.

**Solución (gradual, sin reescribir todo):**
1. Seguir extrayendo módulos a `js/modules/` (uno por vez, mismo patrón que Spotify) antes de intentar pasar a ES modules de verdad.
2. Extraer el núcleo compartido (`S`, `save`, `refresh`, helpers de formato/escapado) a `js/core/` — recién ahí tiene sentido convertir todo a `<script type="module">` con `import`/`export` real.
3. Namespace único (`S.encargos.xxx` en vez de funciones sueltas globales) — ya existe el patrón `S.personas`, `S.ingresosFijos`, extenderlo a todo.
4. Bundler ligero (Vite/esbuild) solo para desarrollo, deployando igual un único `index.html` a GitHub Pages.

**Trade-off:** dividir en módulos hace más fácil depurar un bug aislado, pero más difícil rastrear un bug que se origina en un módulo y se manifiesta en otro, si no se sabe qué módulos comparten estado — el namespace único (punto 3) es lo que evita ese problema, debe ir de la mano con la modularización.

**Nota adicional (sesión posterior) — nueva iniciativa: modularización por pantalla (carga bajo demanda), distinta de la extracción a archivos ya hecha arriba.** Todo lo documentado en este punto hasta acá fue extraer código de `index.html` a archivos `.js` separados — pero los ~36 archivos resultantes se siguen cargando *todos* al arranque (ver punto 4, TBT). El siguiente nivel es cargar cada módulo de dominio solo cuando el usuario entra a esa pantalla, motivado por el Lighthouse real: con datos, "Reduce JS execution time" queda en ~2.6s y el DOM en 4,024 elementos — números que ya no bajan con más ajustes de orden de carga (`async`/`defer`, ver punto 4), solo reduciendo trabajo real.

Antes de diseñar el mecanismo en sí, se hizo un mapeo con `grep` sistemático contra 24 de los ~36 archivos (faltan los core chicos: `events.js`, `diferencial.js`, `split.js`, `money-input.js`, `movimientos.js`, `mas-menu.js`, `sheet-viewport.js`, `sheet-swipe.js`, `gastos-fijos-progress.js`, `mejoras.js`, `mejoras-adicionales.js`, `busqueda-global.js`, `nav.js`, `actividad_reciente.js`, `personas-init.js`, `import-validado.js`, `async-css.js`). Hallazgo central: **`refresh()` en `core-state.js` — invocada después de cada `save()` en toda la app — llamaba sin ningún `typeof` guard a 12 funciones `render*` de 6 módulos de dominio distintos** (`renderDetalleCuenta`/`renderMovsCustom`/`renderCajitas`/`renderCustomCuentasList`/`renderEncargosEnCuenta` de cuentas.js/encargos.js, `renderGastosVar`/`renderGastosFijos`/`renderMesFiltros` de gastos.js, `renderDeudoresList` de prestado.js, `renderMesada` de mesada.js, `renderSpotify` de spotify.js, `renderAttencion` de inicio.js, `renderTCDashboard` de tarjetas_credito.js). Esto bloqueaba de raíz cualquier carga bajo demanda: no cargar un solo módulo de dominio de entrada rompía `refresh()` para toda la app (`ReferenceError`, confirmado con jsdom contra el archivo real). Se corrigió agregando los `typeof` guards faltantes — mismo patrón defensivo que ya usaba `showScreen()` — sin cambiar el comportamiento actual (todo sigue cargando de entrada, como siempre; el único efecto es que `refresh()` ya no asume que un módulo específico existe). Detalle completo, incluidos los otros dos hallazgos del mapeo (el patrón de "parchar la función original" en 8 de los 24 archivos ya vistos, y Personas confirmado como librería transversal a 4 dominios, no una pantalla más), en `CHANGELOG.md#modularización-por-pantalla--mapeo-y-fase-05`.

**Solución (gradual, sin reescribir todo) — lista extendida con la nueva iniciativa:**
1. Seguir extrayendo módulos a `js/modules/` (uno por vez, mismo patrón que Spotify) antes de intentar pasar a ES modules de verdad.
2. Extraer el núcleo compartido (`S`, `save`, `refresh`, helpers de formato/escapado) a `js/core/` — recién ahí tiene sentido convertir todo a `<script type="module">` con `import`/`export` real.
3. Namespace único (`S.encargos.xxx` en vez de funciones sueltas globales) — ya existe el patrón `S.personas`, `S.ingresosFijos`, extenderlo a todo.
4. Bundler ligero (Vite/esbuild) solo para desarrollo, deployando igual un único `index.html` a GitHub Pages.
5. **(Nuevo) Carga bajo demanda por pantalla** — fase 0.5 (blindar `refresh()` y `navTo()`) y fase 0 (mapeo completo, 40/40 archivos) dadas. **Fase 1 (piloto) dada: Alcancía**, con `js/core/lazy-loader.js` (`Loader.ensure(grupo)`, carga en orden por grupo, cachea) enganchado en `showScreen()`. Diseño clave: en vez de continuar la ejecución después de cargar, el loader vuelve a invocar `showScreen(name)` desde cero — así cubre tanto a los módulos que se integran con una rama `if(name===X)` como a los que se auto-registran parchando `showScreen()` ellos mismos (caso de Alcancía, descubierto recién al diseñar el enganche). De paso se encontró y corrigió un tercer caso del bug de `refresh()`: 4 ramas de `showScreen()` (`config`, `analisis`, `personas`, `cuentas`) llamaban funciones de otros módulos sin `typeof` guard. Validado con jsdom (10 checks); falta la prueba en navegador real. Detalle completo, y la lista de próximos candidatos a pantalla lazy, en `CHANGELOG.md`.

### 4. Rendimiento (parcialmente resuelto)

**Resuelto** (ver CHANGELOG para el detalle): Font Awesome/Google Fonts ya no bloquean el render, `preconnect`/`modulepreload` a los dominios de Firebase, minificación disponible como paso de build. Medido: **LCP 7.4s → 5.2s**.

**Sigue pendiente — y es la causa principal de lo que queda:**
- **La UI real depende de que termine toda la cadena de Auth/Firestore antes de mostrarse.** Ya existe una pantalla de carga visible al instante (logo + "Cargando Mis Finanzas"), pero el contenido real de la app (lo que Lighthouse mide como LCP) no aparece hasta que `onAuthStateChanged` → PIN gate → `_fbLoadData()` → `_launchApp()` terminan en cadena. Es el cambio de mayor riesgo de toda la auditoría (puede introducir carreras entre el PIN gate y el auth) — **se está resolviendo de a poco, paso por paso, no de una sola vez.** Ver la nota siguiente para el primer paso ya dado.

> **Paso 1 (sesión posterior, dado):** mapeo completo de la cadena real de arranque con `core-state.js`, `firebase-init.js`, `firebase-sync.js`, `pin-bio.js`, `sheet-stack.js` y `bootstrap.js` en mano. Hallazgo no documentado hasta ahora: `firebase-init.js` es `<script type="module">` sin `async`, así que el navegador lo trata como `defer` — **no ejecuta hasta terminar de parsear TODO `<body>`**, sin importar que esté arriba de todo en el `<head>`. Mientras tanto, los ~30 `<script>` clásicos de la app (`core-state.js`, cada módulo de pantalla, `sheet-stack.js`, etc.) sí bloquean el parser en su posición y se ejecutan de inmediato. Resultado real: **la app entera se parsea/ejecuta antes de que Firebase siquiera arranque** — en serie, no en paralelo, aunque ambas cosas no dependen una de la otra en ese punto.
>
> Fix dado: se agregó `async` al `<script>` de `firebase-init.js`, para que la resolución de auth de Firebase (I/O de red/IndexedDB) corra en paralelo con el JS clásico en vez de esperar a que termine. Como red de seguridad barata — mismo espíritu que `window._pendingPinGate`/`window._authgateReadyTimeout`, ya existentes — se envolvió el callback de `onAuthStateChanged` (que sí toca DOM: `#fb-loading-screen`, `#fb-login-screen`, etc.) con un guard de `document.readyState`: si el documento todavía se está parseando cuando el callback dispara, espera a `DOMContentLoaded` antes de tocar nada. En la práctica no debería activarse nunca (Firebase tarda más en resolver el estado de auth que lo que tarda el parser en llegar a esas dos divs, las primeras del `<body>`), pero sin el guard sería exactamente el tipo de carrera que ya causó el bug real de `window.Events` vs `typeof Events` (ver `CHANGELOG.md#infraestructura--seguridad`). Detalle completo en `CHANGELOG.md#arranque`.
>
> **Lo que este paso NO resuelve** (para no perder de vista el resto): el TBT de ~9s en sí no baja — el JS clásico sigue pesando lo mismo y sigue bloqueando el parser. Lo que gana es que la resolución de Firebase deja de sumarse en serie después de ese bloqueo. La solución completa sigue siendo dividir el JS por pantalla (punto 3, mismo esfuerzo, beneficio directo acá).
>
> **Siguientes pasos posibles, en orden de riesgo creciente:**
> 1. ~~Mismo tratamiento (`async` + guard de `readyState`) para `firebase-sync.js` y `pin-bio.js`~~ — **dado, paso 2.** El mapeo encontró dos riesgos que el paso 1 no tenía: `firebase-sync.js` tenía wiring del overlay "Eliminar cuenta" a nivel superior del módulo sin null-checks (confirmado con jsdom que revienta con `TypeError` si el DOM no está listo — corta la ejecución del resto del archivo, incluyendo el registro de `Events('authgate',...)`), y tanto ese archivo como `pin-bio.js` registraban `Events('authgate'/'pin', ...)` con un guard `typeof Events` que antes nunca fallaba (porque sin `async` el navegador ya garantizaba que `events.js` había cargado) pero que, al fallar con `async`, no tenía ningún reintento — el namespace quedaba sin registrar para siempre, en silencio. Se corrigieron los tres puntos (null-checks + guard de `readyState` para el wiring del overlay y para `window._pendingPinGate`; reintento cada 200ms para ambos registros de `Events`, mismo patrón que ya usaba `pin-bio.js` para su hook de `refresh()`). Detalle completo en `CHANGELOG.md#arranque`.
> 2. ~~Mover el JS clásico pesado (`core-state.js` en adelante) a `defer`~~ — **dado, paso 3, motivado por un Lighthouse real en producción (Performance 43).** El reporte mostró algo que no se sabía hasta tener el dato real: cada uno de los ~36 `<script>` clásicos no empezaba a *descargarse* hasta que el anterior terminaba de *ejecutar* (`js/modules/inicio.js`, el último del bloque, no arrancaba su fetch hasta los 9,460 ms) — no solo la ejecución estaba en serie, la descarga también. Se agregó `defer` a los 36 `<script>` (sin tocar `async-css.js` ni los 3 módulos de Firebase, que ya están en `async`). Menor riesgo real que los pasos 1 y 2 pese a tocar más archivos: `defer` preserva el orden del documento (a diferencia de `async`), y como todos estos scripts están físicamente después de todo el markup real de la app, no cambia nada respecto a qué DOM tienen disponible al ejecutar — solo cambia que las descargas ahora corren en paralelo. Se verificaron las dos restricciones de orden documentadas en el código (`sheet-stack.js` después de `gastos.js`/`spotify.js`, antes de `deudores-personas.js`) — como no se reordenó ningún `<script>`, siguen cumplidas. **Confirmado con Lighthouse real (corrida limpia, sin extensiones de navegador): Element render delay 11,710→3,300 ms (-72%), main-thread work 26.6s→9.0s (-66%), score 43→48.** Detalle completo en `CHANGELOG.md#arranque`.
> 3. ~~Pintar con el caché local de inmediato en `_fbLoadData()` en vez de esperar hasta 8s la confirmación del servidor~~ — **dado, paso 4.** Cambio quirúrgico en `firebase-sync.js` únicamente (no toca auth ni PIN gate): antes, la primera entrega de `onSnapshot` desde caché (`fromCache=true`) se ignoraba a propósito — se esperaba hasta 8s al servidor "para no mostrar datos obsoletos", con el caché solo como fallback si el servidor no respondía a tiempo. Se cambió para pintar con lo que llegue primero (caché o servidor) y reconciliar si el servidor confirma después con algo distinto, por el mismo camino que ya usábamos para actualizaciones en tiempo real de otro dispositivo. Se eliminó el timer de fallback de 8s (`window._fbCacheFallbackTimer`), ya innecesario. Validado con `node --check` + una simulación aislada en Node de la máquina de estados (4 escenarios). **Falta la prueba en navegador real con Firestore de verdad** — no se puede simular con Node solo. Detalle completo en `CHANGELOG.md#arranque`.
> 4. Dividir el JS por pantalla (la solución de fondo, punto 3 de arriba) — recién con eso baja el TBT en sí (26.6s de trabajo total en el hilo principal en la medición de referencia), no solo el orden/paralelismo de carga ni el tiempo hasta el primer pintado. **4 de 11 candidatos originales confirmados y dados: `alcancia`, `comprometida`, `config`(+`import-validado`), `historial`.** Del resto: `mesada`, `spotify`(+`spotify-personas`), `prestado`(+`prestado-personas`+`deudores-personas`), `tarjetas_credito`, `cuentas`, `analisis` y `encargos`(+`encargos-personas`) quedan descartados sin refactor previo — `inicio.js` (el dashboard, siempre eager) llama funciones de esos directo, sin `typeof` guard, para armar el resumen que se renderiza en cada boot. `gastos`(+`gastos-fijos-progress`) también queda descartado, por un motivo distinto y más duro: `sheet-stack.js` (núcleo) parchea `addGastoVar`/`addGastoFijo` a nivel superior de su propio archivo — el `ReferenceError` ocurriría en cada carga de la app, no solo al usar Gastos. Arreglarlo significa diferir ese parche dentro de `sheet-stack.js` (el propio archivo ya muestra el patrón correcto al lado, con el override de `addSpotify`) — un cambio real pero que toca un núcleo con restricciones de orden ya documentadas como frágiles, no se hizo sin que se pida explícitamente. `historial` (`actividad_reciente.js`) necesitó, además de sacar el `<script>`, un cambio real dentro del propio archivo: sus 3 triggers de auto-render (`DOMContentLoaded`, `appDataLoaded`, clic) fallan todos con carga tardía — se agregó un cuarto trigger sin condición de evento. No queda ningún candidato inconcluso. Detalle completo de todo el análisis en `CHANGELOG.md#arranque`.
>
> **⚠️ Paso 5 (sesión posterior, diagnóstico con Lighthouse real del sitio ya desplegado) — el cuello de botella real, con datos: no es el JS.** El usuario reportó que el score no mejoraba (dio 40) pese a los pasos 1-4 y pidió diagnóstico. Con el reporte completo en mano: **latencia máxima de la ruta crítica de 6.464 ms**, dominada por los canales `Listen`/`Write` de Firestore (4.1s-6.5s cada uno) y por `auth/iframe.js` (5.678 ms) — no por JS: `inicio.js` no empieza a ejecutar hasta el ms 8.467 en ese trace, el JS ya está listo hace rato. El elemento LCP (badge de cupo de TC en el dashboard) tiene 3.6s de "render delay" — depende de que los datos reales de Firestore lleguen, no de código. **Sospechoso principal:** `experimentalForceLongPolling: true` en `firebase-init.js` (agregado para evitar `ERR_QUIC_PROTOCOL_ERROR`) fuerza a Firestore a negociar el canal en tiempo real por long-polling en vez de WebChannel normal — varios round-trips HTTP en vez de uno. Alternativa recomendada por Firebase: `experimentalAutoDetectLongPolling: true`. **✅ Cambiado (sesión posterior)** — una sola línea en `firebase-init.js`. Sigue faltando la validación en navegador real conectado a Firestore (no se puede simular con Node/jsdom) y existe riesgo real de reintroducir el `ERR_QUIC_PROTOCOL_ERROR` original; si eso pasa, el revert es igual de simple. **Para poder detectarlo sin estar mirando la consola en vivo:** se agregó registro persistente en `localStorage` (`firebase-sync.js`, handler de error de `onSnapshot`) — últimos 20 errores de conexión con timestamp, código y mensaje, consultables desde la consola con `_verSyncErrors()`. Ver `CHANGELOG.md#diagnóstico--lighthouse-real-sitio-ya-desplegado-el-cuello-de-botella-real-no-es-el-js`. Sí se corrigieron los `preconnect` (2 desperdiciados según el mismo reporte, 1 nuevo agregado) — cambio de solo `<link>`, sin riesgo. Detalle completo en `CHANGELOG.md#arranque`.

> **Nota adicional (2026-08-02) — se destraba `inicio.js` y se da el quinto grupo lazy: `mesada`.** De los 6 candidatos descartados en el paso 4 solo por culpa de `inicio.js` (`mesada`, `spotify`, `prestado`, `tarjetas_credito`, `cuentas`, `analisis`), se agregó guard `typeof === 'function'` a las 6 llamadas que no lo tenían dentro de `inicio.js` (`getDeudorSaldo`, `getMesadaData` ×2, `_getCuotaAnio` ×2, `tcCupoUsadoPct`, `spPersonaPagadaVigente`+`spNombreDe`, `calcDeudaTcPropia` ×2) — mismo patrón que ya usaban `calcPatrimonioTotal`/`totalPrestadoPendiente`/`calcC`/`calcCDT` en ese mismo archivo. No cambia el comportamiento actual (todo sigue cargando de entrada); lo que habilita es que esos módulos puedan volverse lazy sin romper el dashboard. De paso se endureció `js/core/personas-init.js`: el `setTimeout` fijo (600ms/1000ms) que llamaba a `_inyectarPersonaSheets()` sin guard se reemplazó por un guard `typeof` + reintento cada 200ms (mismo patrón que `Events('authgate'/'pin', ...)`, ver más arriba) — necesario para que el bootstrap de Personas no dependa de que sus módulos de integración carguen en un orden fijo si alguno de ellos se vuelve lazy más adelante. `node --check` sin errores en los tres archivos.
>
> Con el destrabe confirmado, se dio **`mesada` como quinto grupo lazy**: no monkey-patchea nada ajeno, su wiring de botones ya corría top-level contra el DOM estático (sin esperar `DOMContentLoaded`), y **se asumió, sin volver a comprobarlo contra el archivo real, que `refresh()` ya tenía guard para `renderMesada` desde el mapeo original (paso 0.5)** — esa asunción resultó falsa, ver corrección del 2026-08-02 más abajo. `#screen-mesada`/`#mas-mesada` ya eran HTML estático — no hubo que inyectar nada. Se agregó `mesada: ['js/modules/mesada.js']` a `Loader.GROUPS` y se sacó su `<script>` de la carga de entrada.
>
> **🔴 Corrección (2026-08-02):** al recibir `core-state.js` real por primera vez desde este cambio, la app rompió en consola con `_getCuotaAnio is not defined` (en `load()`/`save()`) y `renderMesada is not defined` (en `refresh()`). Contra el archivo real: ninguna de las 12 llamadas que el CHANGELOG documenta como blindadas en la Fase 0.5 (`renderDetalleCuenta`, `renderMovsCustom`, `renderCajitas`, `renderCustomCuentasList`, `renderEncargosEnCuenta`, `renderGastosVar`, `renderGastosFijos`, `renderMesFiltros`, `renderDeudoresList`, `renderMesada`, `renderSpotify`, `renderAttencion`, `renderTCDashboard`) tiene guard `typeof` — solo `_refreshCajitaDet` (agregado después, por otro motivo) lo tiene. Tampoco `load()`/`save()` fueron auditadas nunca contra `mesada.js` — llaman a `_getCuotaAnio()` sin guard, 2 veces cada una. Detalle completo del hallazgo y el fix en `CHANGELOG.md`, entrada del 2026-08-02.
>
> **`spotify`, `prestado`, `cuentas` y `analisis` siguen descartados** — el guard de `inicio.js` ya no es el bloqueo para ellos, pero no se auditaron esta sesión contra sus propios archivos (no se recibieron) para confirmar que no tienen el mismo tipo de acoplamiento oculto que ya apareció en Cuentas→Encargos.
>
> **`tarjetas_credito` — intento y hallazgo nuevo, no se dio.** Se corrigió dentro del archivo un bug real independiente del guard de `inicio.js`: el wiring de sus botones (`btn-guardar-tc`, `ptc_fuente`, etc.) esperaba `'DOMContentLoaded'`, que con carga lazy ya disparó mucho antes de que el archivo llegue a existir — mismo bug ya encontrado y corregido en `actividad_reciente.js` (grupo `historial`). Se movió a wiring top-level, mismo patrón que `mesada.js`. Pero apareció un bloqueo real, más duro: `tcNormalizarTarjetas()`, definida en `tarjetas_credito.js`, no tiene ningún caller dentro del propio archivo — según su comentario ("se ejecuta en cada `refresh()`"), el caller vive en `core-state.js`, no recibido esta sesión. El mapeo original del paso 0.5 solo blindó 12 funciones `render*`; `tcNormalizarTarjetas` no es una de ellas y no se pudo confirmar si tiene guard. Si no lo tiene, volver `tarjetas_credito` lazy rompería `refresh()` en cada boot hasta que el usuario entre a la pantalla — no se fuerza el cambio sin poder confirmarlo. Queda como hallazgo abierto: hace falta `core-state.js` para cerrarlo.
>
> Balance actualizado de los 11 candidatos originales: **5 dados** (`alcancia`, `comprometida`, `config`, `historial`, `mesada`), **1 bloqueado por su propio núcleo** (`tarjetas_credito`, hallazgo nuevo esta sesión — antes estaba en el mismo grupo que los descartados por `inicio.js`, ahora es un caso aparte), **5 sin auditar contra sus propios archivos** (`spotify`+`spotify-personas`, `prestado`+`prestado-personas`+`deudores-personas`, `cuentas`, `analisis`, `encargos`+`encargos-personas`), **1 descartado por un núcleo distinto** (`gastos`+`gastos-fijos-progress`, sin cambios). `node --check` sin errores en `mesada.js`, `tarjetas_credito.js` y `lazy-loader.js`. **Sigue pendiente la prueba en navegador real** para `mesada` — jsdom no está disponible esta sesión para validar contra el `index.html`/`lazy-loader.js` reales como se hizo con los grupos anteriores.
>
> **Nota adicional (2026-08-02, continuación) — se recibió `core-state.js` real y se cierran/reabren varios de los puntos de arriba.**
>
> 1. **`tcNormalizarTarjetas()` — la duda quedó resuelta: SÍ tenía guard `typeof` desde antes** (`if(typeof tcNormalizarTarjetas==='function') tcNormalizarTarjetas();` en `refresh()`). El hallazgo de la sesión anterior ("hace falta `core-state.js` para cerrarlo") se cierra en ese punto puntual. Pero al revisar el resto de `refresh()` contra el archivo real apareció el bloqueo real de fondo, ya anticipado por la Corrección del mismo día: **de las 13 llamadas `render*` de la Fase 0.5, solo `renderMesada` había sido blindada** (en el fix de `_getCuotaAnio`) — las otras 12 (`renderDetalleCuenta`, `renderMovsCustom`, `renderCajitas`, `renderCustomCuentasList`, `renderEncargosEnCuenta`, `renderGastosVar`, `renderGastosFijos`, `renderMesFiltros`, `renderDeudoresList`, `renderSpotify`, `renderAttencion`, `renderTCDashboard`) seguían sin guard, incluida `renderTCDashboard` — un segundo bloqueo real para `tarjetas_credito`, independiente de `tcNormalizarTarjetas`. **Se agregó guard `typeof` a las 12** (mismo patrón, quirúrgico, sin tocar ningún módulo de dominio). Con esto, `refresh()` ya no asume que ningún módulo de pantalla esté cargado — cierra el lado de `core-state.js` para los 6 candidatos que quedaban pendientes de este archivo (`tarjetas_credito`, `spotify`, `prestado`, `cuentas`, `encargos`, y de paso `gastos`/`inicio`, aunque esos dos no eran candidatos activos esta sesión).
> 2. **Hallazgo nuevo, fuera de la lista de 13:** `calcPatrimonioTotal()` (llamada desde `save()` vía `snapshotPatrimonio()` — en CADA `save()` de la app, no solo en `refresh()`) llamaba a `getDeudorSaldoPatrimonio(d)` (`prestado.js`) sin guard. Mismo tipo de bloqueo que `tcNormalizarTarjetas`, pero para `prestado` — no estaba en el mapeo original de Fase 0.5 porque esa fase solo auditó `refresh()`, no `save()`/`calcPatrimonioTotal()`. **Se agregó el guard.** Con este fix, `core-state.js` ya no tiene ninguna llamada sin guard a `prestado.js`, `spotify.js`, `cuentas.js`, `encargos.js`, `gastos.js`, `inicio.js` ni `tarjetas_credito.js` — confirmado con `grep` sistemático contra los nombres de función de los 13 + `getDeudorSaldoPatrimonio`. `node --check` sin errores.
> 3. **`spotify.js` y `spotify-personas.js` auditados línea por línea contra el mismo criterio que Cuentas→Encargos** (el que motivó este pedido): sin `.innerHTML` sin escapar (todo lo que interpola texto libre — `spNombreDe`, `h.nombre`, `h.nota` — ya pasa por `escHtml()`) y sin `onclick`/`onchange`/`oninput` inline (ya migrados a `Events`, ambos archivos limpios). **Pero sí apareció un acoplamiento oculto real, del mismo tipo que Cuentas→Encargos:** el flujo "pagar Spotify con tarjeta de crédito" (`confirmarPagarSpotify()` y el preview de cupo en `actualizarSpPagarPreview()`) llama a `getTCById`/`tcCupoDisponible`/`tcRecalcular` (`tarjetas_credito.js`) **sin ningún guard**, 3 veces. Es distinto a los casos de arriba porque no es una llamada de render de fondo — es una acción explícita del usuario ("pagar con esta tarjeta"), así que envolverla en un `if(typeof...)` silencioso rompería la función en vez de protegerla (el botón se vería habilitado pero no haría nada). **No se fuerza un fix acá** — queda como hallazgo abierto que requiere una decisión de producto (ej. `Loader.ensure('tarjetas_credito')` antes de permitir esa acción, si `spotify` y `tarjetas_credito` alguna vez cargan como grupos lazy independientes). `spotify-personas.js` también depende sin guard de `getPersona`/`abrirSelPersona`/`_inyectarPersonaSheets`/`iniciales` (`personas.js`) — confirmado seguro por ahora porque Personas es transversal (carga siempre eager, no es candidato a lazy) y el orden de `<script defer>` ya garantiza que carga antes.
> 4. **`mesada.js` re-auditado buscando acoplamiento saliente hacia otros módulos de dominio** (spotify, prestado, tarjetas_credito, encargos, personas): ninguno encontrado — confirma la evaluación del 2026-08-02 ("no monkey-patchea nada ajeno").
> 5. **Punto 3 del pedido original (¿`analisis.js` llama algo de `mesada.js` sin guard?) sigue sin poder verificarse** — `analisis.js` no se recibió esta sesión. Lo único nuevo: confirmado que del lado de `mesada.js` no hay nada que dependa de `analisis.js`, así que si hay un problema, está solo del lado de `analisis.js`.
>
> **Balance actualizado:** `tarjetas_credito` pasa de "bloqueado por su propio núcleo" a **bloqueado por su acoplamiento con `spotify.js`** (punto 3 de arriba) — ya no por `core-state.js`. `prestado` gana un hallazgo nuevo y ya resuelto en `core-state.js` (punto 2) pero sigue sin auditar contra su propio archivo (`prestado.js`, `prestado-personas.js`, `deudores-personas.js` no recibidos). `spotify` queda auditado y limpio salvo por el mismo acoplamiento con `tarjetas_credito`. `cuentas`, `analisis`, `encargos` siguen sin auditar contra sus propios archivos. `node --check` sin errores en `core-state.js`, `mesada.js`, `spotify.js`, `spotify-personas.js`.
>
> **Nota adicional (2026-08-02, tercera ronda) — se recibieron `analisis.js`, `cuentas.js`, `encargos.js`, `prestado.js`. Cierra el punto 2 (con matices) y encuentra 2 bugs vivos, no solo hipotéticos.**
>
> **Bugs vivos corregidos** (`mesada` ya es un grupo lazy real desde hace varias sesiones, así que estos no eran hallazgos "para cuando se vuelva lazy" — ya rompían hoy):
> 1. `analisis.js` — las 3 llamadas a `getMesadaData()`/`_getCuotaAnio()` (ingresos estimados del mes, sección "Total mesada" en `#an-mesada-section`, e ingresos del mes anterior) estaban guardadas solo por `S.modulos.mesada` (¿está *activo* el módulo?), no por un guard `typeof` (¿está *cargado* el `.js`?). Confirma exactamente el punto 3 del pedido original. **Fix:** se agregó `&&typeof getMesadaData==='function'` a las 3 condiciones.
> 2. `cuentas.js` — `getMovimientosCuenta()` (la función que arma el historial de movimientos al abrir el detalle de Nequi, Nu o Efectivo) llamaba a `getMesadaData()` **sin ningún guard, ni siquiera el de config**. Este es más grave que el de `analisis.js`: no dependía de ninguna opción — cualquiera que abriera el detalle de una cuenta sin haber visitado Mesada antes se encontraba con un `ReferenceError` a mitad de función. **Fix:** se envolvió el `forEach` con `if(typeof getMesadaData==='function')`.
>
> **Acoplamiento oculto tipo Cuentas→Encargos, confirmado y corregido preventivamente** (`encargos.js` y `cuentas.js` siguen cargando eager hoy, así que esto no rompía nada todavía, pero es exactamente el patrón que motivó este pedido):
> 3. `cuentas.js → encargos.js`: `renderDetalleCuenta()` (nequi/nu/efectivo) y el detalle de cuenta personalizada llaman a `renderEncargosEnCuenta()` — 4 veces, sin guard. **Fix:** las 4 con `typeof`.
> 4. `encargos.js → cuentas.js`: al borrar un movimiento de encargo, se llama a `renderDetalleCuenta()` sin guard (además de la llamada ya guardada que hace `refresh()` — esta es una segunda, directa). También `calcC()` (cuentas.js) sin guard en el cálculo de interés compuesto de la porción de un encargo dentro de una cajita. **Fix:** ambas con `typeof`.
> 5. `prestado.js → cuentas.js`: `calcC()` sin guard en el preview de impacto de un préstamo sobre una cajita con meta de ahorro. **Fix:** con `typeof`, cae al mismo fallback que ya existía para "cajita sin fecha/saldo".
>
> **Confirmado limpio:** ninguno de los 4 archivos nuevos toca `tarjetas_credito.js` (ni siquiera `prestado.js`/`encargos.js`, que sí leen/escriben `S.tarjetasCredito` — pero como datos planos, no como llamadas a función, igual que `descontarFuente`/`sumarFuente` en `core-state.js`). Ninguno toca `alcancia`, `comprometida`, `configuracion` ni `actividad_reciente` (los otros 4 grupos ya lazy). `analisis.js` no depende de `cuentas.js`/`encargos.js`/`prestado.js`. `node --check` sin errores en los 4 archivos.
>
> **Balance final de esta ronda:** de los 5 candidatos que quedaban "sin auditar contra sus propios archivos", **4 quedan cerrados** (`cuentas`, `analisis`, `encargos`, `prestado` — todos con guards agregados donde hacía falta). Solo falta `spotify-personas` (ya auditado, limpio) y las variantes `prestado-personas.js`/`deudores-personas.js`/`encargos-personas.js` (archivos hermanos no recibidos, mismo patrón que `spotify-personas.js` — probablemente solo dependen de `personas.js`, que es transversal y siempre eager, pero no se puede confirmar sin el archivo). El único bloqueo real que queda para dar por "lazy-ready" a `tarjetas_credito` es el acoplamiento con `spotify.js` (ver Nota anterior, punto 3) — pendiente de decisión de producto, no de auditoría.
>
> **Nota adicional (2026-08-02, quinta ronda) — decisión tomada: resolver de raíz el acoplamiento spotify↔tarjetas_credito, no solo documentarlo. Se recibió `lazy-loader.js`. Mitad hecha, mitad bloqueada por un hallazgo nuevo.**
>
> **Hecho — `spotify.js`:** las 3 llamadas a `getTCById`/`tcCupoDisponible`/`tcRecalcular` (preview de pago, confirmar pago, revertir pago al eliminar del historial) ya no asumen que `tarjetas_credito.js` está cargado. Se agregó `_spEnsureTC()` — un helper `async` que llama a `Loader.ensure('tarjetas')` solo si `getTCById` todavía no existe (si `tarjetas_credito.js` sigue cargando eager, es un no-op inmediato) y muestra un `toast` de error si la descarga falla en vez de dejar el botón mudo. `actualizarSpPagarPreview()` y `confirmarPagarSpotify()` pasaron a `async` (`_borrarSpHistorial()` ya lo era). En la reversión de un pago con TC, `mov.eliminado=true` se marca siempre — es solo un dato — pero `tcRecalcular()` (una llamada real a función) espera la carga primero, así el estado nunca queda a medias por una descarga lenta. `node --check` sin errores.
>
> **Bloqueado — volver `tarjetas_credito` un grupo lazy real (agregarlo a `Loader.GROUPS` + sacar su `<script>` eager de `index.html`):** al revisar `index.html` para hacer este cambio apareció un comentario existente (fechado 2026-08-03) que documenta que `tarjetas_credito.js` **también** depende de `js/core/movimientos.js` — usa `abrirDetalleMov` directo en su `Events.registerAll('tarjetas', {...})}` pero carga antes que `movimientos.js` en el orden actual, y ya tuvo que resolverse con un wrapper (mismo patrón que `leerArchivoImport` en `configuracion.js`). No se tocó nada de esto sin ver `tarjetas_credito.js` real: cambiar cuándo carga (de entrada vs. bajo demanda mucho después) podría interactuar con ese wrapper de formas que no se pueden predecir sin el archivo. Tampoco se agregó la entrada a `Loader.GROUPS` todavía — hacerlo sin sacar el `<script>` eager al mismo tiempo arriesgaría una doble carga de `tarjetas_credito.js` si `showScreen('tarjetas')` chequea `Loader.GROUPS` en general (como ya hace con los otros 5), lo que podría tirar `SyntaxError: identificador ya declarado` si el archivo usa `const`/`let` a nivel de módulo. **Falta `js/modules/tarjetas_credito.js` para cerrar esta mitad.**
>
> **Nota adicional (2026-08-02, sexta ronda) — se recibió `js/core/movimientos.js`. Confirma la dirección de acoplamiento que faltaba y encuentra un tercer bug vivo de mesada.**
>
> `movimientos.js` es un archivo del NÚCLEO (no de un dominio) pero, según su propio header, depende de funciones de 3 módulos de dominio — `tcEliminarCompraInterna`/`tcEliminarPagoInterna` (Tarjetas de Crédito), `abrirCustomCuenta`/`renderDetalleCuenta` (Cuentas), `getEncargo` (Encargos) — "todos ya cargados antes de este archivo en index.html". Las 5 llamadas estaban sin ningún guard `typeof`. Se clasificaron en dos categorías, con fixes distintos:
>
> - **`getEncargo` (dentro de `abrirDetalleMov`) y `abrirCustomCuenta`/`renderDetalleCuenta` (al final de `eliminarMovimiento`):** son lookups/renders de apoyo, no mutan nada — se les agregó guard `typeof` simple, igual que en `core-state.js`/`cuentas.js`/`encargos.js`. Si el módulo no está cargado, se saltan sin romper nada.
> - **`tcEliminarCompraInterna`/`tcEliminarPagoInterna` (dentro de `eliminarMovimiento`, rama `gasto`) y `getMesadaData` (misma función, rama `mesada`):** estas SÍ mutan datos financieros reales (revierten deuda de tarjeta, revierten plata a una fuente, borran el registro original en su módulo de origen) — un guard que las salte en silencio dejaría el movimiento borrado del feed general pero SIN revertir su efecto real, un problema de integridad de datos, no solo una UI a medias. Se aplicó el mismo patrón que `_spEnsureTC()` en `spotify.js`: `eliminarMovimiento()` (ya era `async`) espera `Loader.ensure('tarjetas')`/`Loader.ensure('mesada')` si hace falta, y si la carga falla, aborta con un `toast` de error y `return` ANTES de tocar cualquier estado — nunca deja el borrado a medias.
>
> **La rama `mesada` era un bug vivo, no hipotético** — igual que los ya encontrados en `analisis.js` y `cuentas.js`: `mesada` ya es un grupo lazy real, así que borrar un movimiento de mesada desde el feed general de actividad, sin haber visitado antes la pantalla Mesada, rompía con `ReferenceError` a mitad de `eliminarMovimiento()`. Van 3 apariciones del mismo bug en 3 archivos distintos esta ronda de auditoría.
>
> Confirmado limpio el resto del archivo: sin `.innerHTML` sin escapar (todo pasa por `escHtml()`), sin acoplamiento hacia `spotify.js`/`prestado.js` más allá de datos planos en `S.tarjetasCredito`/`S.tcMovimientos` (mismo patrón seguro que `descontarFuente`/`sumarFuente`). `node --check` sin errores.
>
> **Esto deja el lado de `movimientos.js` listo para cuando `tarjetas_credito` se vuelva lazy** — pero sigue faltando `js/modules/tarjetas_credito.js` en sí para: (a) confirmar que su wrapper de `abrirDetalleMov` sigue funcionando si se cambia su momento de carga, y (b) agregar `Loader.GROUPS.tarjetas` + sacar el `<script>` eager de `index.html`, los dos cambios juntos.
>
> **Nota adicional (2026-08-02, séptima ronda) — se recibió `js/modules/tarjetas_credito.js`. CIERRA el tema: `tarjetas_credito` ya es el sexto grupo lazy real.**
>
> El archivo real resolvió las dos dudas que quedaban abiertas:
> - **El wrapper de `abrirDetalleMov`** (en `Events.registerAll('tarjetas', {...})`, `verMov: (...args) => abrirDetalleMov(...args)`) resuelve el nombre global recién al hacer click, no al cargar el archivo — es lazy-safe sin ningún cambio, sin importar cuándo cargue `tarjetas_credito.js` en relación a `js/core/movimientos.js`.
> - **`tcNormalizarTarjetas()`** — el propio header del archivo documentaba la duda ("no tiene caller dentro de este archivo... no se pudo confirmar si `core-state.js` ya tenía guard"). Ya confirmado en la ronda "continuación": sí lo tiene.
>
> Se encontraron y arreglaron 2 llamadas más a `calcC()` (cuentas.js) sin guard — ambas en widgets de "cobertura" (¿la cajita vinculada alcanza para cubrir la deuda?), una en `renderTCDashboard()` y otra en `abrirDetalleTCSheet()`. Ninguna muta datos, así que el fix fue un guard simple que oculta el widget si `cuentas.js` no cargó — con un cuidado extra en `abrirDetalleTCSheet()`: el `else if` original mostraba "la cajita fue eliminada" cuando no había cobertura, y sin ajustarlo ese mensaje habría aparecido incorrectamente cuando la cajita sí existe pero `calcC` todavía no cargó. Se corrigió la condición del `else if` para que solo dispare cuando la cajita de verdad no existe.
>
> **Cambios aplicados:**
> 1. `js/core/lazy-loader.js`: `tarjetas: ['js/modules/tarjetas_credito.js']` agregado a `Loader.GROUPS` como sexto grupo.
> 2. `index.html`: sacado el `<script src="js/modules/tarjetas_credito.js" defer>` eager, reemplazado por el comentario placeholder (mismo patrón que los otros 5 grupos). `#screen-tarjetas` y `#mas-tarjetas` ya eran HTML estático — no hizo falta copiar/inyectar nada.
> 3. `tarjetas_credito.js`: los 2 guards de `calcC()` de arriba, más el comentario de cabecera actualizado para reflejar que ya es lazy.
>
> **Efecto secundario aceptado, no corregido:** el parche a `window.mostrarAlertaFuente` al final de `tarjetas_credito.js` (agrega el hint "se carga a la TC" cuando se elige una tarjeta como fuente en Gastos/Encargos/Préstamos) no se aplica hasta que el usuario visite Tarjetas de Crédito por primera vez y dispare la carga lazy. Antes de eso, elegir una tarjeta como fuente en otra pantalla simplemente no muestra ese hint — no rompe nada, solo no está la mejora hasta la primera visita. No se intentó resolver esto (requeriría que Gastos/Encargos/Préstamos disparen `Loader.ensure('tarjetas')` al abrir su selector de fuente, un cambio mucho más invasivo para un beneficio cosmético).
>
> `node --check` sin errores en `tarjetas_credito.js` y `lazy-loader.js`. **Sigue pendiente la prueba en navegador real** (jsdom no disponible esta sesión) — mismo pendiente que ya existía para el quinto grupo (`mesada`).
>
> **Con esto, el acoplamiento spotify↔tarjetas_credito queda resuelto de raíz, no solo documentado**: `tarjetas_credito` es un grupo lazy real, y los 3 módulos que lo usaban (`spotify.js`, `js/core/movimientos.js`, y el propio `core-state.js`) ya esperan su carga bajo demanda en vez de asumir que está disponible. De los 6 grupos lazy que existen ahora, este es el primero que se resolvió por un bug de acoplamiento ya encontrado en vez de por ser la "próxima pantalla candidata" — vale la pena usar este mismo patrón (`_xxxEnsureModulo()` + `Loader.ensure()` + abortar con `toast` si la mutación es real) la próxima vez que aparezca un caso similar.
>
> **Nota adicional (2026-08-02, octava ronda) — pedido explícito: fusionar `spotify.js` + `spotify-personas.js` en un solo archivo, como ya es `tarjetas_credito.js`. Hecho, con un hallazgo real de por medio.**
>
> El header de `spotify.js` ya advertía por qué estaban partidos: `spotify.js` necesitaba cargar TEMPRANO (antes de `encargos.js`) porque "un par de wirings de botones de otros módulos referencian `addSpotify`/`guardarEditarSpotify` de forma inmediata, no diferida". Se verificó contra los archivos reales en mano (`encargos.js`, `mesada.js`) — de los "un par" solo se confirmó **uno real**: `encargos.js` línea ~1999, `btn_guardar_edit_sp.addEventListener('click', guardarEditarSpotify)` — una referencia directa que exige que `guardarEditarSpotify` ya exista en el momento exacto en que `encargos.js` se ejecuta. `mesada.js` no tiene ninguna referencia a `addSpotify`/`guardarEditarSpotify` — esa parte del comentario original parece desactualizada (no se pudo confirmar contra `inicio.js`, no recibido esta sesión, así que no se descarta al 100% que haya algo similar ahí).
>
> **Fix de raíz, no un workaround:** dos líneas arriba de esa, en el mismo archivo, `crearEncargo` tenía exactamente este problema y ya estaba resuelto con una referencia diferida (`() => crearEncargo()` en vez de `crearEncargo` directo), con un comentario explicando la técnica ("para que cada clic lea el valor ACTUAL de la variable global"). Se aplicó el mismo patrón a la línea de Spotify: `() => guardarEditarSpotify()`. Con eso, `guardarEditarSpotify` ya no necesita existir en el momento en que `encargos.js` se ejecuta — solo necesita existir para cuando el usuario haga click en ese botón, mucho después de que todos los `<script defer>` ya cargaron.
>
> **Con esa dependencia real eliminada, no quedó ninguna razón para mantener los archivos separados.** Se fusionaron: el contenido de `spotify-personas.js` se agregó al final de `spotify.js`, bajo una sección "INTEGRACIÓN SPOTIFY ↔ PERSONAS" — se mantuvieron los 2 `Events.registerAll('spotify', {...})` como llamadas separadas (una por sección) en vez de fusionarlos en un solo objeto, para no arriesgar ningún cambio de comportamiento más allá de la fusión en sí. El archivo resultante (un solo `spotify.js`) se movió a la posición donde antes vivía `spotify-personas.js` (después de `personas.js` y `encargos-personas.js`) — no a la posición vieja de `spotify.js` — porque esa es la mitad más exigente de las dos: la integración con Personas sí necesita que `openSheet`/`getPersona`/`abrirSelPersona`/`_inyectarPersonaSheets` ya existan al momento de sus propios monkey-patches de nivel superior (`const _origX = X;`).
>
> **Cambios aplicados:**
> 1. `encargos.js` — referencia diferida para `guardarEditarSpotify`, mismo patrón que `crearEncargo`.
> 2. `spotify.js` — fusión completa con el contenido de `spotify-personas.js`; header reescrito documentando la fusión y por qué ya no hace falta el archivo aparte; comentarios de "REGISTRO DE EVENTOS" actualizados (ya no dicen "el resto vive en spotify-personas.js").
> 3. `index.html` — sacado el `<script>` viejo de `spotify.js` (posición temprana); el `<script>` de `spotify-personas.js` (posición tardía) ahora apunta al `spotify.js` fusionado; comentario del bloque de Personas actualizado (ya no lista a Spotify entre los módulos que cargan aparte).
> 4. `spotify-personas.js` **queda obsoleto — borrar el archivo del repo**, ya no lo referencia nada.
>
> `node --check` sin errores en `encargos.js` y `spotify.js`. **No se tocó `mesada.js` ni ningún otro archivo** más allá de estos tres. Sigue sin poder confirmarse al 100% que no haya una referencia inmediata similar en `inicio.js` (no recibido) — si algún día aparece un `ReferenceError` de `guardarEditarSpotify`/`addSpotify` al cargar la app, ese archivo es el primer sospechoso.
>
> **Cierre (2026-08-02, novena ronda) — se recibió `inicio.js`. Confirmado: cero referencias a `addSpotify`/`guardarEditarSpotify`, la fusión queda 100% validada.** Las únicas dos funciones de Spotify que `inicio.js` toca (`spPersonaPagadaVigente` y `spNombreDe`, para el widget de alertas del dashboard) ya estaban guardadas con `typeof` desde la sesión anterior — mismo patrón que el resto de las llamadas de este archivo hacia mesada/TC/prestado/cuentas, todas ya confirmadas con guard. No quedó ninguna duda abierta sobre la fusión de `spotify.js` + `spotify-personas.js`.
>
> **Nota adicional (2026-08-02, cuarta ronda) — se recibieron los 3 archivos hermanos restantes: cierra el punto 2 por completo, sin necesidad de fixes.**
>
> `deudores-personas.js`, `encargos-personas.js` y `prestado-personas.js` auditados con el mismo criterio: sin `.innerHTML` sin escapar (los usos son templates estáticos o texto fijo, sin interpolar campos libres), sin handlers inline (ya migrados), y **sin ningún acoplamiento hacia mesada/cuentas/encargos/spotify/tarjetas_credito** — confirma la hipótesis de la ronda anterior: los 3 solo dependen de `personas.js` (transversal, siempre eager) y de su propio dominio. Orden de carga verificado contra `index.html` (`personas.js` → `encargos-personas.js` → `spotify-personas.js` → `prestado-personas.js` → `deudores-personas.js`) y consistente con lo que cada header declara — en particular, `deudores-personas.js` dice explícitamente que necesita cargar después de `prestado-personas.js` porque envuelve un `crearMiDeuda` ya envuelto por ese archivo, y así es en el HTML real.
>
> **No hizo falta ningún fix.** Dos hallazgos menores, ninguno bloqueante:
> - Lógica de vinculación de persona duplicada en la cadena de monkey-patches de `crearMiDeuda` (`prestado-personas.js` vincula por coincidencia de nombre, `deudores-personas.js` sobreescribe después con el `personaId` ya seleccionado) — el resultado final es correcto porque el segundo gana, pero es trabajo redundante y en un caso límite de dos personas homónimas podría vincular momentáneamente a la persona equivocada antes de corregirse.
> - Posible código muerto: el wrap de `addDeudor()` en `prestado-personas.js` está pensado para el viejo sheet con input `np_nombre`, pero `deudores-personas.js` ahora intercepta `openSheet('nueva-persona')` y abre directo el selector de personas — el sheet viejo podría ya no ser alcanzable desde la UI actual. No se pudo confirmar sin ver el botón que dispara `addDeudor()` en `index.html`.
>
> **Con esto, el punto 2 del pedido original ("spotify, prestado, cuentas, analisis, encargos — auditarlos contra sus propios archivos") queda cerrado en su totalidad**, incluidos los 4 archivos hermanos (`spotify-personas`, `encargos-personas`, `prestado-personas`, `deudores-personas`). Lo único que queda abierto de toda la ronda de auditoría de lazy-loading es la decisión de producto sobre el acoplamiento `spotify.js`↔`tarjetas_credito.js`.

- **TBT (Total Blocking Time) medido en 8,980 ms** — muy alto, el hilo principal bloqueado casi 9 segundos. Misma causa raíz que el punto 3 (monolito): todo el JS de las 13 pantallas se parsea/ejecuta de una sola vez aunque el usuario solo esté viendo una. La minificación ayuda al tamaño de descarga pero no reduce el trabajo de ejecución en sí — eso solo se resuelve dividiendo el código por pantalla (mismo esfuerzo que el punto 3, con beneficio directo acá).
- **JS no usado:** ~526 KiB según la corrida más reciente de Lighthouse — mismo problema, todo se carga aunque no se use.
- **Font Awesome sin subsetear:** sigue cargándose completo desde cdnjs. No se pudo self-hostear un subconjunto de íconos en esta ronda por falta de acceso de red al CDN desde el entorno de trabajo — queda pendiente, hacerlo requiere bajar y re-alojar los archivos de fuente reales. Reporte real de Lighthouse (sesión posterior): 18.4 KiB de CSS sin usar solo en `all.min.css`.
- **Cache lifetimes (~344 KiB de ahorro estimado según Lighthouse real, sesión posterior):** todos los archivos de GitHub Pages sirven con TTL de 10 minutos — no configurable desde el código sin un CDN (ej. Cloudflare) delante de GitHub Pages.
- **`js/core/async-css.js` bloquea el render** (sesión posterior, Lighthouse real) pese a que su trabajo es justamente volver no-bloqueante Font Awesome/Google Fonts — el `<script>` en sí no tiene `defer`. Impacto chico (1.4 KiB) pero no se tocó: no se subió ese archivo esta sesión, hace falta confirmar que `defer` no rompe el truco `media="print"→"all"` que usa.
- **Tamaño del DOM:** 3.285 elementos en el reporte más reciente (bajó de los 4.024/4.021 de mediciones anteriores — consistente con que los grupos lazy no inyectan su HTML hasta que se visitan) y 20 long tasks.

---



## 🟡 Advertencias — pendientes

| Problema | Estado |
|---|---|
| Sin tests | Pendiente. Priorizar funciones de cálculo puras: `calcPatrimonioTotal()`, amortización, `calcHealthScore()`. |
| ~~Solo 11 `aria-label` en toda la app~~ | ✅ **Resuelto esta sesión.** Se identificaron 22 botones ícono-solo (sin texto visible) sin `aria-label` en `index.html` — Cuentas (volver ×4, editar/eliminar cuenta personalizada, volver a Nu, eliminar cajita, volver a Meta/CDTs de cajita), Préstamos (volver deudores, editar/eliminar deudor, volver mis deudas, editar/eliminar mi deuda), Mesada (año anterior/siguiente), Encargos (volver, editar/eliminar) y el botón borrar del teclado del PIN. Se les agregó `aria-label` a los 22 (reusando el texto del `title` ya existente donde lo había). Verificado con un barrido programático sobre todos los `<button>` del archivo: 0 restantes sin `aria-label` que tengan solo ícono. Ver `CHANGELOG.md#infraestructura--seguridad`. |
| ~~CSS inline (44.7 KB)~~ | ✅ **Resuelto esta sesión.** Se extrajeron los dos bloques `<style>` grandes (CSS principal de layout/componentes, ~46 KB, y el bloque "MEJORAS UX" de ~1.8 KB) a `styles.css` externo, cargado con `<link rel="stylesheet">` bloqueante (a propósito — es el layout base, no hay nada coherente que mostrar sin él; lo no-bloqueante hubiera causado FOUC). Queda inline, a propósito, un tercer `<style>` chico (override `font-display:swap` de Font Awesome, ~7 líneas) — depende de aparecer justo después del `<link>` de cdnjs para ganarle en cascada; no vale la pena el round-trip de red extra ni el riesgo de reordenarlo por algo tan chico. **`style-src 'unsafe-inline'` sigue en la CSP** por ese fragmento — esto no lo cierra, solo reduce el CSS inline de 44.7 KB a ~200 bytes. Verificado: llaves `{`/`}` de `styles.css` balanceadas (450/450), `<head>`/`</head>`/`<body>`/`</body>` balanceados en `index.html`. `index.html` bajó de 5.249 a 4.663 líneas (-586). **Falta la prueba visual en navegador real** (que no haya FOUC ni clases sin estilo) antes de dar esto por cerrado. |
| Imágenes base64 embebidas (3, ~1.4 KB) | Impacto bajo, sigue sin ser prioridad. |
| Service Worker (`sw.js`) | ✅ **Confirmado funcionando en producción** — se sacó de esta tabla, ver CHANGELOG. |

---

## ⚪ Puntos descartados (falsos positivos de revisiones anteriores)

- ~~"4 marcadores TODO/FIXME/HACK"~~ — descartado, eran la palabra "TODO" dentro de texto normal.
- ~~"Funciones muertas: `onOk`, `hideTT`, `addSpotify`, `importarJSON`"~~ — descartado, las cuatro están en uso.
- **"Patrón 'Dividir ÷' repetido"** — dejó de ser un hallazgo de arquitectura: al revisarlo de cerca, ya existía un sistema compartido (`crearSplitWidget`) usado en 4 de los módulos. Lo que sí había eran bugs puntuales de sincronización de color en los módulos que no usaban ese sistema (Préstamos, Encargos) — corregidos, ver CHANGELOG bajo esos dos módulos. No se unificó todo bajo `crearSplitWidget` porque no hacía falta: el problema real era más chico de lo que parecía.

---

## Priorización sugerida (actualizada)

1. ~~Los ~9 pares `onmouseenter`/`onmouseleave` sin ubicación exacta y el gate de PIN/login~~ — **cerrado esta sesión**, ver `CHANGELOG.md#infraestructura--seguridad`: 0 `onclick`/hover inline en todo el archivo.
2. ~~Externalizar los 3 bloques `<script>` inline que quedaban (`S`/`save()`, sheet-stack, `refresh()`)~~ — **cerrado esta sesión**: de 18 bloques a 0 (ver punto 1 arriba y `CHANGELOG.md#infraestructura--seguridad`); `firebase-init.js`/`firebase-sync.js` ya se habían resuelto antes con un guard de "botón listo". *Esto sigue cerrado* — lo que se reabrió es un punto distinto:
   - ~~Revisar todos los módulos con atributos `on*` generados dinámicamente vía `innerHTML`, y auditar los 5 archivos de infraestructura~~ — **cerrado**: los 29 archivos de la app confirmados, `'unsafe-inline'` fuera de `script-src` — ver `CHANGELOG.md#infraestructura--seguridad`.
3. **Reestructurar el arranque para no depender de toda la cadena de Auth/Firestore antes del primer contenido real** — pasos 1-4 dados (ver sección "4. Rendimiento" arriba), `experimentalAutoDetectLongPolling` ya cambiado (falta validar en navegador real), falta probar el paso 4 en navegador real y, de fondo, dividir el JS por pantalla (punto 4 de abajo) para bajar el TBT en sí. Sigue el cambio de mayor impacto en rendimiento que queda, y el de mayor riesgo. Hacer con pruebas en vivo paso a paso, no de una sola vez.
4. **Modularizar por dominio** (resuelve monolito, JS no usado, TBT y tamaño del DOM a la vez — mismo trabajo que el punto 3 en buena parte).
5. Self-hostear un subconjunto de Font Awesome (requiere acceso al CDN para bajar los archivos reales).
6. ~~Extraer CSS a archivo externo~~ — **dado esta sesión**, ver tabla de advertencias arriba y `CHANGELOG.md#infraestructura--seguridad`. Ahora despliega dos archivos (`index.html` + `styles.css`) en vez de uno solo — tenlo presente al subir a GitHub Pages: ambos tienen que subirse juntos.
7. Segunda pasada de auditoría de `.innerHTML` si se agregan campos de texto libre nuevos al modelo de datos.
8. Tests unitarios de las funciones de cálculo financiero puras.

> **Nota de verificación (esta sesión), útil para cuando se ataque el punto 4:** `S` (línea ~4835), `save()` (~5183) y `refresh()` (~6474) siguen viviendo directo en `index.html`, sin mover a `js/core/` — confirma que el punto 4 no se ha empezado. Hallazgo puntual encontrado de paso: `window.refresh` no es una sola función, son **tres reasignaciones que se envuelven en cadena** (líneas 6474, 7282 y 7771 — cada módulo que se cargó después redefine `window.refresh` guardando una referencia a la versión anterior y llamándola antes de agregar su propio re-render). Es una forma válida de que módulos independientes se enganchen a un evento global sin tocarse entre sí, pero también es exactamente el tipo de acoplamiento disperso que el punto 4 busca resolver con un namespace único — vale la pena tenerlo mapeado antes de mover `refresh()` a `js/core/`, para no romper esa cadena de wraps.
